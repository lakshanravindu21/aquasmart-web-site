import React, { useState } from "react";import { NavLink } from "react-router-dom";import { Menu, X, Waves } from "lucide-react";const NAV = [  { to: "/", label: "Home" },  { to: "/problem", label: "Problem" },  { to: "/solution", label: "Solution" },  { to: "/architecture", label: "Architecture" },  { to: "/ai-models", label: "AI Models" },  { to: "/results", label: "Results" },  { to: "/ui-screens", label: "UI Screens" },  { to: "/tech", label: "Tech Stack" },  { to: "/team", label: "Team" },];function cn(...c) {  return c.filter(Boolean).join(" ");}export default function Header() {  const [open, setOpen] = useState(false);  const linkClass = ({ isActive }) =>    cn(      "rounded-xl px-3 py-2 text-sm font-semibold transition",      isActive ? "bg-slate-900 text-white" : "text-slate-700 hover:bg-slate-100"    );  return (    <header className="sticky top-0 z-50 border-b border-slate-200 bg-white/75 backdrop-blur">      <div className="mx-auto flex w-full max-w-6xl items-center justify-between px-4 py-3 md:px-6">        <NavLink to="/" className="flex items-center gap-2">          <div className="grid h-10 w-10 place-items-center rounded-xl bg-gradient-to-br from-blue-600 to-cyan-500 text-white shadow-sm">            <Waves size={18} />          </div>          <div className="leading-tight">            <div className="text-sm font-extrabold text-slate-900">AquaSmart</div>            <div className="text-[11px] text-slate-500">AIoT Aquaculture Research</div>          </div>        </NavLink>        <nav className="hidden items-center gap-1 md:flex">          {NAV.map((n) => (            <NavLink key={n.to} to={n.to} className={linkClass}>              {n.label}            </NavLink>          ))}        </nav>        <div className="flex items-center gap-2">          <NavLink            to="/results"            className="hidden rounded-xl bg-gradient-to-r from-blue-600 to-indigo-600 px-4 py-2 text-sm font-bold text-white shadow-sm hover:opacity-95 md:inline-flex"          >            View Results          </NavLink>          <button            className="grid h-10 w-10 place-items-center rounded-xl border border-slate-200 bg-white md:hidden"            onClick={() => setOpen((v) => !v)}            aria-label="Toggle menu"          >            {open ? <X size={18} /> : <Menu size={18} />}          </button>        </div>      </div>      {open ? (        <div className="border-t border-slate-200 bg-white md:hidden">          <div className="mx-auto w-full max-w-6xl px-4 py-3">            <div className="grid gap-2">              {NAV.map((n) => (                <NavLink                  key={n.to}                  to={n.to}                  onClick={() => setOpen(false)}                  className={({ isActive }) =>                    cn(                      "rounded-xl px-3 py-2 text-sm font-semibold",                      isActive ? "bg-slate-900 text-white" : "bg-slate-50 text-slate-700"                    )                  }                >                  {n.label}                </NavLink>              ))}            </div>          </div>        </div>      ) : null}    </header>  );}