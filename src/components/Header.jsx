import React, { useEffect, useState } from "react";import { NavLink, useLocation } from "react-router-dom";import { Menu, X, Waves } from "lucide-react";const NAV = [  { to: "/", label: "Home" },  { to: "/problem", label: "Problem" },  { to: "/solution", label: "Solution" },  { to: "/architecture", label: "Architecture" },  { to: "/ai-models", label: "AI Models" },  { to: "/results", label: "Results" },  { to: "/ui-screens", label: "UI Screens" },  { to: "/tech", label: "Tech Stack" },  { to: "/team", label: "Team" },];function cn(...c) {  return c.filter(Boolean).join(" ");}// -- Fluid value: linearly interpolate between minVal@minW and maxVal@maxW --function fluid(minVal, maxVal, minW, maxW, w) {  if (w <= minW) return minVal;  if (w >= maxW) return maxVal;  return minVal + ((maxVal - minVal) * (w - minW)) / (maxW - minW);}export default function Header() {  const [open, setOpen] = useState(false);  const location = useLocation();  useEffect(() => setOpen(false), [location.pathname]);  const [w, setW] = useState(    typeof window !== "undefined" ? window.innerWidth : 1400  );  useEffect(() => {    const onResize = () => setW(window.innerWidth);    onResize();    window.addEventListener("resize", onResize);    return () => window.removeEventListener("resize", onResize);  }, []);  // -- Breakpoints ----------------------------------------------------------  const isCollapsed = w < 1000;   // hamburger takes over below 1000px    // -- Fluid nav font-size: 12px @ 1000px -> 15px @ 1300px ------------------  // Increased slightly since Monitor button is gone  const navFontSize = fluid(12, 15, 1000, 1300, w);  // -- Fluid nav px padding: 10px @ 1000px -> 16px @ 1300px ------------------  const navPx = fluid(10, 16, 1000, 1300, w);  // -- Fluid gap between nav items: 2px @ 1000px -> 6px @ 1300px ------------  const navGap = fluid(2, 6, 1000, 1300, w);  // -- Brand visibility logic ------------------  const showBadge = w >= 1150;  const showSubtitle = w >= 1200;  useEffect(() => {    if (!isCollapsed) setOpen(false);  }, [isCollapsed]);  return (    <header className="sticky top-0 z-50 shadow-2xl transition-all duration-300">      {/* -- BACKGROUND -- */}      <div className="absolute inset-0 bg-gradient-to-r from-slate-950 via-blue-950 to-indigo-950/90 backdrop-blur-xl border-b border-white/10" />      {/* -- GLOW BLOBS -- */}      <div className="pointer-events-none absolute -top-16 left-1/3 h-40 w-80 bg-cyan-500/15 blur-[120px] animate-pulse" />      <div className="pointer-events-none absolute -bottom-16 right-1/4 h-40 w-80 bg-indigo-500/15 blur-[120px]" />      <div className="relative mx-auto w-full max-w-[1400px] px-4 sm:px-6 lg:px-8">        <div className="flex min-h-20 items-center justify-between gap-3">          {/* -- BRANDING -- */}          <NavLink            to="/"            className="group flex shrink-0 items-center gap-3 animate-in fade-in slide-in-from-left-6 duration-700"          >            {/* Logo icon — fluid size */}            <div              className="relative grid shrink-0 place-items-center overflow-hidden rounded-2xl bg-gradient-to-br from-cyan-500 via-blue-600 to-indigo-700 text-white shadow-[0_0_20px_rgba(6,182,212,0.3)] transition-all duration-300 group-hover:rotate-12 group-hover:scale-110"              style={{                width:  fluid(38, 44, 1000, 1300, w),                height: fluid(38, 44, 1000, 1300, w),              }}            >              <Waves size={fluid(18, 22, 1000, 1300, w)} />              <div className="absolute inset-0 bg-white/20 opacity-0 transition-opacity group-hover:opacity-100" />            </div>            {/* Brand text */}            <div className="leading-tight">              <div className="flex items-center gap-2">                <span                  className="font-black tracking-tight text-white drop-shadow-[0_2px_4px_rgba(0,0,0,0.5)] whitespace-nowrap transition-all duration-300"                  style={{ fontSize: fluid(17, 22, 1000, 1300, w) }}                >                  AquaSmart                </span>                {/* Badge */}                {showBadge && (                  <span className="shrink-0 rounded-full bg-cyan-400/20 border border-cyan-400/40 px-2.5 py-0.5 text-[10px] font-black uppercase tracking-widest text-cyan-300 whitespace-nowrap animate-in fade-in zoom-in-50 duration-500">                    Group 16                  </span>                )}              </div>              {/* Subtitle */}              {showSubtitle && (                <div className="text-[10px] font-bold uppercase tracking-[0.2em] text-blue-400/80 whitespace-nowrap animate-in fade-in slide-in-from-left-2 duration-700">                  IoT Precision Research                </div>              )}            </div>          </NavLink>          {/* -- DESKTOP NAV (Visible >= 1000px) -- */}          {!isCollapsed && (            <nav              className="flex items-center transition-all duration-500"              style={{ gap: navGap }}            >              {NAV.map((n, index) => (                <NavLink                  key={n.to}                  to={n.to}                  className="relative group/link"                >                  {({ isActive }) => (                    <span                      className={cn(                        "relative inline-flex items-center rounded-xl font-black tracking-wide transition-all duration-300 whitespace-nowrap",                        isActive                          ? "text-cyan-400 bg-white/10 shadow-[inset_0_0_10px_rgba(255,255,255,0.05)]"                          : "text-white/90 hover:text-white hover:bg-white/5"                      )}                      style={{                        fontSize:      navFontSize,                        paddingLeft:   navPx,                        paddingRight:  navPx,                        paddingTop:    fluid(6, 8, 1000, 1300, w),                        paddingBottom: fluid(6, 8, 1000, 1300, w),                      }}                    >                      {n.label}                      <span                        className={cn(                          "absolute bottom-0 left-2 right-2 h-[2.5px] rounded-full transition-all duration-500",                          isActive                            ? "bg-gradient-to-r from-cyan-400 to-blue-500 opacity-100 shadow-[0_0_12px_rgba(34,211,238,0.8)]"                            : "opacity-0 group-hover/link:opacity-40 group-hover/link:bg-white/50"                        )}                      />                    </span>                  )}                </NavLink>              ))}            </nav>          )}          {/* -- RIGHT ACTIONS (Hamburger only) -- */}          <div className="flex shrink-0 items-center">            {isCollapsed && (              <button                className="grid h-11 w-11 place-items-center rounded-xl border border-white/20 bg-white/10 text-white transition-all duration-300 hover:bg-white/20 active:scale-95 shadow-lg"                onClick={() => setOpen((o) => !o)}                aria-label={open ? "Close menu" : "Open menu"}                aria-expanded={open}              >                <span                  className={cn(                    "absolute transition-all duration-300",                    open ? "opacity-100 rotate-0 scale-100" : "opacity-0 rotate-90 scale-50"                  )}                >                  <X size={22} />                </span>                <span                  className={cn(                    "absolute transition-all duration-300",                    open ? "opacity-0 rotate-90 scale-50" : "opacity-100 rotate-0 scale-100"                  )}                >                  <Menu size={22} />                </span>              </button>            )}          </div>        </div>        {/* -- MOBILE MENU PANEL -- */}        <div          style={{            display:    isCollapsed ? "block" : "none",            maxHeight:  open ? "90vh" : "0px",            opacity:    open ? 1 : 0,            overflow:   "hidden",            transition: "max-height 500ms cubic-bezier(0.4,0,0.2,1), opacity 400ms ease",            paddingBottom: open ? "2.5rem" : "0",          }}        >          <div className="rounded-3xl border border-white/10 bg-slate-950/95 p-4 shadow-[0_20px_50px_rgba(0,0,0,0.5)] backdrop-blur-3xl">            <div className="grid grid-cols-1 min-[420px]:grid-cols-2 gap-3">              {NAV.map((n) => (                <NavLink key={n.to} to={n.to}>                  {({ isActive }) => (                    <div                      className={cn(                        "flex flex-col items-center justify-center gap-2 rounded-2xl py-4 transition-all duration-300",                        isActive                          ? "text-white bg-gradient-to-br from-blue-600 to-indigo-700 shadow-xl scale-[1.02] border border-white/20"                          : "text-white/70 bg-white/5 border border-white/5 hover:bg-white/10 hover:text-white"                      )}                    >                      <span className="px-3 text-center text-xs font-black uppercase tracking-widest">                        {n.label}                      </span>                      {isActive && (                        <div className="h-1.5 w-1.5 rounded-full bg-cyan-400 shadow-[0_0_10px_rgba(34,211,238,1)]" />                      )}                    </div>                  )}                </NavLink>              ))}            </div>                        {/* Added Monitor button to mobile menu since it's removed from header */}            <div className="mt-4">              <NavLink                to="/results"                className="inline-flex w-full items-center justify-center rounded-2xl bg-gradient-to-r from-cyan-500 via-blue-600 to-indigo-600 px-6 py-3 text-xs font-black uppercase tracking-[0.2em] text-white shadow-[0_4px_15px_rgba(37,99,235,0.3)] transition-all duration-300 hover:brightness-110 active:scale-[0.99]"              >                Launch Dashboard              </NavLink>            </div>          </div>        </div>      </div>    </header>  );}