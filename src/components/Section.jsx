import React from "react";function cn(...c) {  return c.filter(Boolean).join(" ");}export function Badge({ children }) {  return (    <span className="inline-flex items-center rounded-full border border-slate-200 bg-white/70 px-3 py-1 text-xs font-semibold text-slate-700 shadow-sm">      {children}    </span>  );}export function Card({ children, className }) {  return (    <div className={cn("rounded-2xl border border-slate-200 bg-white p-6 shadow-sm", className)}>      {children}    </div>  );}export default function Section({ title, subtitle, eyebrow, children }) {  return (    <section className="py-12 md:py-16">      <div className="mx-auto w-full max-w-6xl px-4 md:px-6">        <div className="mb-8">          {eyebrow ? (            <div className="mb-3">              <Badge>{eyebrow}</Badge>            </div>          ) : null}          <h2 className="text-2xl font-extrabold tracking-tight md:text-3xl">{title}</h2>          {subtitle ? <p className="mt-3 max-w-3xl text-sm text-slate-600 md:text-base">{subtitle}</p> : null}        </div>        {children}      </div>    </section>  );}