import React, { useState, useEffect, useRef } from "react";import Layout from "../components/Layout.jsx";import PageTransition from "../components/PageTransition.jsx";import Section, { Card, Badge } from "../components/Section.jsx";import {  BrainCircuit,  Layers,  Target,  Cpu,  Database,  Zap,  LineChart,  Microscope,  Layers3,  CheckCircle2,  BarChart3,  ScanLine,  Sigma,  GitBranch,  Clock,  ArrowRight,  ShieldCheck,  FlaskConical,  Waves,  Eye,  Network,} from "lucide-react";/* -- floating particle canvas (same as Solution.jsx) ----------- */function AIParticleCanvas() {  const ref = useRef(null);  useEffect(() => {    const canvas = ref.current;    if (!canvas) return;    const ctx = canvas.getContext("2d");    let raf;    const resize = () => {      canvas.width = canvas.offsetWidth;      canvas.height = canvas.offsetHeight;    };    resize();    window.addEventListener("resize", resize);    const COUNT = 55;    const pts = Array.from({ length: COUNT }, () => ({      x: Math.random() * canvas.width,      y: Math.random() * canvas.height,      r: Math.random() * 2.8 + 0.8,      vx: (Math.random() - 0.5) * 0.4,      vy: (Math.random() - 0.5) * 0.4,      a: Math.random() * 0.6 + 0.2,    }));    const draw = () => {      ctx.clearRect(0, 0, canvas.width, canvas.height);      for (let i = 0; i < pts.length; i++) {        for (let j = i + 1; j < pts.length; j++) {          const dx = pts[i].x - pts[j].x;          const dy = pts[i].y - pts[j].y;          const dist = Math.sqrt(dx * dx + dy * dy);          if (dist < 160) {            ctx.beginPath();            ctx.strokeStyle = `rgba(99, 102, 241, ${0.22 * (1 - dist / 160)})`;            ctx.lineWidth = 1.0;            ctx.moveTo(pts[i].x, pts[i].y);            ctx.lineTo(pts[j].x, pts[j].y);            ctx.stroke();          }        }      }      pts.forEach((p) => {        ctx.beginPath();        ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);        ctx.fillStyle = `rgba(129, 140, 248, ${p.a * 0.7})`;        ctx.fill();        p.x += p.vx;        p.y += p.vy;        if (p.x < 0 || p.x > canvas.width) p.vx *= -1;        if (p.y < 0 || p.y > canvas.height) p.vy *= -1;      });      raf = requestAnimationFrame(draw);    };    draw();    return () => {      cancelAnimationFrame(raf);      window.removeEventListener("resize", resize);    };  }, []);  return <canvas ref={ref} className="absolute inset-0 w-full h-full" />;}/* -- model modules data ----------- */const models = [  {    icon: LineChart,    title: "LSTM Forecasting Model",    subtitle: "Temporal Dependency Analysis",    desc: "A Long Short-Term Memory (LSTM) network designed to handle non-linear water quality fluctuations. It utilizes a 10-step rolling look-back window to predict lethal trends 24 hours in advance.",    color: "#2563eb",    lightBg: "#eff6ff",    borderColor: "#bfdbfe",    num: "01",    subFeatures: [      { icon: Database, label: "17,000+ Training Points" },      { icon: Clock, label: "10-Step Look-back Window" },      { icon: Waves, label: "24-Hour Advance Forecast" },    ],  },  {    icon: Microscope,    title: "MobileNetV2 Classifier",    subtitle: "Pathological Vision Engine",    desc: "A lightweight Convolutional Neural Network (CNN) optimized for mobile-edge inference. It identifies WSSV and Black Gill symptoms with high precision using inverted residual bottlenecks.",    color: "#7c3aed",    lightBg: "#f5f3ff",    borderColor: "#ddd6fe",    num: "02",    subFeatures: [      { icon: GitBranch, label: "Transfer Learning Applied" },      { icon: Eye, label: "Edge-Optimized Inference" },      { icon: Layers3, label: "Healthy / WSSV / BG Classes" },    ],  },  {    icon: Sigma,    title: "Soft-Sensor Framework",    subtitle: "Mathematical Parameter Derivation",    desc: "To optimize hardware costs, AquaSmart utilizes mathematical derivation logic within the Node.js backend. This allows for high-accuracy estimation of complex parameters like DO and NH₃ from primary physical sensor data.",    color: "#059669",    lightBg: "#ecfdf5",    borderColor: "#a7f3d0",    num: "03",    subFeatures: [      { icon: Cpu, label: "Cost-Optimized Design" },      { icon: FlaskConical, label: "DO & NH₃ Derivation" },      { icon: Zap, label: "Real-time Processing" },    ],  },];/* -- model card (mirrors ModuleCard from Solution.jsx) ----------- */function ModelCard({ p }) {  const [hovered, setHovered] = useState(false);  const Icon = p.icon;  return (    <div      className="bg-white rounded-3xl border flex flex-col overflow-hidden transition-all duration-500 h-full"      style={{        borderColor: hovered ? p.color : p.borderColor,        boxShadow: hovered          ? `0 20px 40px rgba(0,0,0,0.06), 0 0 0 1px ${p.color}22`          : "0 2px 10px rgba(0,0,0,0.02)",        transform: hovered ? "translateY(-5px)" : "translateY(0)",      }}      onMouseEnter={() => setHovered(true)}      onMouseLeave={() => setHovered(false)}    >      <div className="h-1.5 w-full" style={{ backgroundColor: p.color }} />      <div className="p-6 sm:p-8 flex flex-col flex-grow">        <div className="flex items-center justify-between mb-5">          <div            className="w-12 h-12 sm:w-14 sm:h-14 rounded-2xl flex items-center justify-center border transition-all duration-300"            style={{              backgroundColor: p.lightBg,              borderColor: hovered ? `${p.borderColor}aa` : "#e2e8f0",            }}          >            <Icon size={24} style={{ color: p.color }} />          </div>          <span            className="text-3xl sm:text-4xl font-black select-none transition-colors duration-300"            style={{ color: hovered ? p.color : "#e2e8f0" }}          >            {p.num}          </span>        </div>        <p          className="text-[10px] font-black uppercase tracking-[0.25em] mb-2"          style={{ color: p.color }}        >          {p.subtitle}        </p>        <h3 className="text-lg sm:text-xl font-extrabold text-slate-900 mb-3 leading-tight">          {p.title}        </h3>        <p className="text-sm sm:text-base text-slate-600 leading-relaxed flex-grow">          {p.desc}        </p>        <div className="mt-6 pt-5 border-t border-slate-100 grid grid-cols-1 gap-2">          {p.subFeatures.map((sub, i) => {            const SubIcon = sub.icon;            return (              <div                key={i}                className="flex items-center gap-3 px-3.5 py-2.5 rounded-xl transition-all duration-200"                style={{                  background: `linear-gradient(135deg, ${p.lightBg} 0%, white 100%)`,                  border: `1px solid ${p.borderColor}`,                }}              >                <div                  className="w-6 h-6 rounded-lg flex items-center justify-center flex-shrink-0"                  style={{ backgroundColor: `${p.color}18` }}                >                  <SubIcon size={13} style={{ color: p.color }} />                </div>                <span className="text-xs sm:text-sm font-semibold text-slate-700">                  {sub.label}                </span>              </div>            );          })}        </div>      </div>    </div>  );}/* -- pipeline step ----------- */function PipelineStep({ icon: Icon, label, sublabel, color, lightBg, borderColor, isLast }) {  const [hovered, setHovered] = useState(false);  return (    <div className="flex items-center gap-2 sm:gap-3 flex-1 min-w-0">      <div className="flex flex-col items-center flex-1 min-w-0">        <div          className="w-full rounded-2xl border p-3 sm:p-4 flex flex-col items-center gap-2 text-center transition-all duration-300 cursor-default"          style={{            backgroundColor: hovered ? lightBg : "#ffffff",            borderColor: hovered ? color : borderColor,            boxShadow: hovered ? `0 8px 24px ${color}18` : "0 2px 8px rgba(0,0,0,0.03)",            transform: hovered ? "translateY(-3px)" : "translateY(0)",          }}          onMouseEnter={() => setHovered(true)}          onMouseLeave={() => setHovered(false)}        >          <div            className="w-9 h-9 rounded-xl flex items-center justify-center"            style={{ backgroundColor: `${color}15` }}          >            <Icon size={18} style={{ color }} />          </div>          <p className="text-[10px] sm:text-xs font-black text-slate-800 uppercase tracking-tight leading-tight">{label}</p>          <p className="text-[9px] sm:text-[10px] font-medium text-slate-400 leading-tight hidden sm:block">{sublabel}</p>        </div>      </div>      {!isLast && (        <div className="flex-shrink-0 text-slate-300">          <ArrowRight size={14} />        </div>      )}    </div>  );}export default function AIModels() {  return (    <Layout>      <PageTransition>        <div className="bg-slate-50 min-h-screen">          <div className="mx-auto w-full max-w-[1400px] px-4 sm:px-6 lg:px-12 py-10 md:py-16 lg:py-20">            {/* ── HEADER ─────────────────────────────────────── */}            <div              className="relative mb-12 md:mb-16 overflow-hidden rounded-2xl sm:rounded-3xl border border-slate-200 shadow-sm"              style={{ background: "linear-gradient(135deg, #ffffff 0%, #f8fafc 70%, #f1f5f9 100%)" }}            >              <div                className="absolute left-0 top-0 bottom-0 w-1 sm:w-1.5 rounded-l-2xl sm:rounded-l-3xl"                style={{ background: "linear-gradient(180deg, #2563eb 0%, #7c3aed 50%, #059669 100%)" }}              />              <div                className="absolute -top-16 -right-16 w-64 h-64 rounded-full pointer-events-none"                style={{ background: "radial-gradient(circle, rgba(99,102,241,0.12) 0%, transparent 70%)" }}              />              <div                className="absolute -bottom-12 left-1/3 w-48 h-48 rounded-full pointer-events-none"                style={{ background: "radial-gradient(circle, rgba(124,58,237,0.08) 0%, transparent 70%)" }}              />              <AIParticleCanvas />              <div className="relative z-10 px-6 sm:px-10 md:px-14 lg:px-16 py-8 sm:py-10 md:py-11">                <div className="inline-flex items-center gap-2 px-3 sm:px-4 py-1.5 rounded-full mb-6 sm:mb-8 bg-white border border-slate-200 shadow-inner">                  <BrainCircuit size={12} className="text-indigo-500" />                  <span className="text-slate-500 text-[10px] font-black uppercase tracking-[0.3em]">                    Artificial Intelligence Layer                  </span>                </div>                <div className="grid grid-cols-1 md:grid-cols-2 gap-8 md:gap-12 lg:gap-16 items-center">                  <div>                    <h1                      className="font-black text-slate-950 leading-tight"                      style={{ fontSize: "clamp(1.75rem, 5vw, 3.5rem)", letterSpacing: "-0.03em" }}                    >                      Predictive &{" "}                      <span className="text-indigo-600">Diagnostic</span>{" "}                      Intelligence.                    </h1>                  </div>                  <div className="flex flex-col gap-5">                    <p className="text-slate-600 text-sm sm:text-base md:text-lg leading-relaxed">                      AquaSmart leverages dual-model reasoning to provide 24-hour                      forecasting windows and instant pathological identification,                      shifting management from reactive observation to proactive                      intervention.                    </p>                    <div className="flex flex-wrap gap-2">                      {[                        { label: "LSTM Forecasting", bg: "#eff6ff", border: "#bfdbfe", color: "#2563eb" },                        { label: "MobileNetV2 CNN", bg: "#f5f3ff", border: "#ddd6fe", color: "#7c3aed" },                        { label: "Soft-Sensor Derivation", bg: "#ecfdf5", border: "#a7f3d0", color: "#059669" },                      ].map((chip, i) => (                        <span                          key={i}                          className="text-xs font-bold px-3 py-1.5 rounded-full"                          style={{ backgroundColor: chip.bg, border: `1px solid ${chip.border}`, color: chip.color }}                        >                          {chip.label}                        </span>                      ))}                    </div>                  </div>                </div>              </div>            </div>            {/* ── GRID DIVIDER ───────────────────────────────── */}            <div className="flex items-center gap-3 mb-8 md:mb-10">              <div className="h-px flex-1 bg-slate-200" />              <span className="text-[10px] sm:text-xs font-semibold uppercase tracking-widest text-slate-400 px-2 text-center">                3 Core AI Models              </span>              <div className="h-px flex-1 bg-slate-200" />            </div>            {/* ── MODELS GRID ───────────────────────────────── */}            <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5 sm:gap-6 lg:gap-8 mb-10 md:mb-14">              {models.map((m, idx) => (                <ModelCard key={idx} p={m} />              ))}            </div>            {/* ── RESEARCH OUTCOME ─────────────────────────── */}            <div className="relative overflow-hidden rounded-2xl sm:rounded-3xl border border-slate-200 bg-white shadow-lg">              {/* Top gradient accent */}              <div                className="h-1.5 w-full"                style={{ background: "linear-gradient(90deg, #2563eb 0%, #7c3aed 50%, #059669 100%)" }}              />              {/* Decorative bg */}              <div className="absolute inset-0 pointer-events-none overflow-hidden">                <div                  className="absolute -top-24 -right-24 w-96 h-96 rounded-full"                  style={{ background: "radial-gradient(circle, rgba(99,102,241,0.06) 0%, transparent 70%)" }}                />                <div                  className="absolute -bottom-20 -left-20 w-80 h-80 rounded-full"                  style={{ background: "radial-gradient(circle, rgba(5,150,105,0.05) 0%, transparent 70%)" }}                />                <div className="absolute top-0 right-0 p-8 opacity-[0.03]">                  <BrainCircuit size={220} className="text-slate-900" />                </div>              </div>              <div className="relative z-10 p-8 sm:p-10 md:p-12 lg:p-16">                <p className="text-[10px] font-black uppercase tracking-[0.3em] text-slate-400 mb-2">                  Research Design                </p>                <h2 className="text-xl sm:text-2xl md:text-3xl font-black text-slate-900 mb-10 sm:mb-12 tracking-tight">                  How the AI Layer Works Together                </h2>                {/* ── Pipeline flow ── */}                <div className="mb-10 sm:mb-12">                  <p className="text-[10px] font-black uppercase tracking-[0.25em] text-slate-400 mb-5">                    End-to-End Inference Flow                  </p>                  <div className="flex items-stretch gap-2 sm:gap-3">                    {[                      { icon: Waves,       label: "Sensor Input",   sublabel: "Raw telemetry",     color: "#0891b2", lightBg: "#ecfeff", borderColor: "#a5f3fc" },                      { icon: Sigma,       label: "Soft-Sensor",    sublabel: "DO & NH₃ derived",  color: "#059669", lightBg: "#ecfdf5", borderColor: "#a7f3d0" },                      { icon: LineChart,   label: "LSTM Engine",    sublabel: "24h forecast",      color: "#2563eb", lightBg: "#eff6ff", borderColor: "#bfdbfe" },                      { icon: Microscope,  label: "CNN Classifier", sublabel: "Disease detection", color: "#7c3aed", lightBg: "#f5f3ff", borderColor: "#ddd6fe" },                      { icon: ShieldCheck, label: "Risk Decision",  sublabel: "Actuator trigger",  color: "#dc2626", lightBg: "#fef2f2", borderColor: "#fecaca" },                    ].map((step, i, arr) => (                      <PipelineStep key={i} {...step} isLast={i === arr.length - 1} />                    ))}                  </div>                </div>                {/* ── Bottom two-column ── */}                <div className="grid grid-cols-1 md:grid-cols-2 gap-6 sm:gap-8">                  {/* Left: narrative */}                  <div                    className="rounded-2xl border p-6 sm:p-8 flex flex-col justify-between"                    style={{ backgroundColor: "#f8fafc", borderColor: "#e2e8f0" }}                  >                    <div>                      <p className="text-[10px] font-black uppercase tracking-[0.25em] text-slate-400 mb-3">                        System Rationale                      </p>                      <p className="text-sm sm:text-base text-slate-700 leading-relaxed font-medium">                        AquaSmart's three AI components are intentionally decoupled yet tightly orchestrated. The Soft-Sensor layer reduces hardware dependency. The LSTM model shifts interventions from reactive to predictive. The CNN classifier provides visual diagnostics without laboratory infrastructure — making precision aquaculture accessible at the farm edge.                      </p>                    </div>                    <div className="mt-6 pt-5 border-t border-slate-200">                      <p className="text-[10px] font-black uppercase tracking-[0.25em] text-slate-400 mb-3">                        Core Design Goal                      </p>                      <p className="text-sm font-semibold text-slate-600 leading-relaxed">                        Eliminate the "Reactive Gap" — the critical window between water quality deterioration and human intervention — through continuous autonomous AI monitoring.                      </p>                    </div>                  </div>                  {/* Right: design pillars */}                  <div className="flex flex-col gap-3">                    {[                      {                        icon: Network,                        color: "#2563eb",                        lightBg: "#eff6ff",                        borderColor: "#bfdbfe",                        label: "Heterogeneous Data Fusion",                        desc: "Time-series telemetry and image data are processed by specialized models, each optimized for its data modality.",                      },                      {                        icon: Cpu,                        color: "#7c3aed",                        lightBg: "#f5f3ff",                        borderColor: "#ddd6fe",                        label: "Edge-Cloud Hybrid Inference",                        desc: "Lightweight CNN runs at the device edge; LSTM forecasting is handled cloud-side via the Flask bridge for scalability.",                      },                      {                        icon: ShieldCheck,                        color: "#059669",                        lightBg: "#ecfdf5",                        borderColor: "#a7f3d0",                        label: "Cost-Constrained Architecture",                        desc: "Soft-sensor derivation replaces expensive physical probes, keeping the full AIoT stack deployable for small-scale coastal farmers.",                      },                    ].map((pillar, i) => {                      const PillarIcon = pillar.icon;                      return (                        <div                          key={i}                          className="flex gap-4 rounded-2xl border p-4 sm:p-5"                          style={{                            background: `linear-gradient(135deg, ${pillar.lightBg} 0%, white 100%)`,                            borderColor: pillar.borderColor,                          }}                        >                          <div                            className="w-9 h-9 rounded-xl flex items-center justify-center flex-shrink-0 mt-0.5"                            style={{ backgroundColor: `${pillar.color}18` }}                          >                            <PillarIcon size={17} style={{ color: pillar.color }} />                          </div>                          <div>                            <p className="text-xs font-black text-slate-800 uppercase tracking-tight mb-1">                              {pillar.label}                            </p>                            <p className="text-xs sm:text-sm text-slate-500 font-medium leading-relaxed">                              {pillar.desc}                            </p>                          </div>                        </div>                      );                    })}                  </div>                </div>              </div>            </div>          </div>        </div>      </PageTransition>    </Layout>  );}