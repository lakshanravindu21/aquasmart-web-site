import React from "react";import Layout from "../components/Layout.jsx";import PageTransition from "../components/PageTransition.jsx";import Section, { Card } from "../components/Section.jsx";import { AlertTriangle, Clock, Activity } from "lucide-react";export default function Problem() {  const items = [    {      icon: <Clock size={18} />,      title: "Manual monitoring gaps",      desc: "Once/twice daily checks create blind spots where lethal spikes happen unnoticed.",    },    {      icon: <AlertTriangle size={18} />,      title: "Delayed disease diagnosis",      desc: "WSSV and Black Gill can spread quickly; lab confirmation takes time.",    },    {      icon: <Activity size={18} />,      title: "No closed-loop response",      desc: "Alerts alone are not enough—ponds require automatic stabilization via actuators.",    },  ];  return (    <Layout>      <PageTransition>        <Section          eyebrow="Context"          title="Problem Statement"          subtitle="Traditional pond management is reactive and vulnerable to sudden water quality changes and disease outbreaks."        >          <div className="grid gap-4 md:grid-cols-3">            {items.map((x) => (              <Card key={x.title}>                <div className="flex items-center gap-2">                  <span className="text-rose-600">{x.icon}</span>                  <div className="text-sm font-extrabold">{x.title}</div>                </div>                <p className="mt-2 text-sm text-slate-600">{x.desc}</p>              </Card>            ))}          </div>          <div className="mt-6 rounded-3xl border border-slate-200 bg-gradient-to-br from-rose-50 to-white p-6 shadow-sm">            <div className="text-sm font-extrabold">Research Motivation</div>            <p className="mt-2 text-sm text-slate-600 md:text-base">              AquaSmart addresses the need for real-time monitoring, predictive intelligence, and automation in              Sri Lankan prawn aquaculture to reduce mortality risk and improve operational stability.            </p>          </div>        </Section>      </PageTransition>    </Layout>  );}