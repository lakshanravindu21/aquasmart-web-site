import React from "react";import Layout from "../components/Layout.jsx";import PageTransition from "../components/PageTransition.jsx";import Section, { Card, Badge } from "../components/Section.jsx";import ImageFrame from "../components/ImageFrame.jsx";import { Server, Wifi, Cpu, Database, Zap } from "lucide-react";export default function Architecture() {  const base = import.meta.env.BASE_URL;  return (    <Layout>      <PageTransition>        <Section          eyebrow="System Design"          title="Multi-Tier Architecture & Dual Backend Orchestration"          subtitle="Distributed intelligence: edge acquisition on ESP32-CAM, cloud reasoning via Node.js + Flask, realtime database, and automation feedback loop."        >          <div className="grid gap-6 md:grid-cols-2">            <Card>              <div className="flex items-start justify-between gap-4">                <div>                  <div className="text-sm font-extrabold">Figure 3.1</div>                  <div className="mt-1 text-xs text-slate-500">Edge → Tunnel → Dual Backend → UI → Automation</div>                </div>                <Badge>Core</Badge>              </div>              <div className="mt-4">                <ImageFrame                  src={`${base}assets/figure-3-1.png`}                  alt="System architecture"                  caption="Figure 3.1: Multi-Tier System Architecture"                />              </div>            </Card>            <Card>              <div className="flex items-start justify-between gap-4">                <div>                  <div className="text-sm font-extrabold">Figure 4.1</div>                  <div className="mt-1 text-xs text-slate-500">ESP32-CAM + sensors + relay module pin mapping</div>                </div>                <Badge>Hardware</Badge>              </div>              <div className="mt-4">                <ImageFrame                  src={`${base}assets/figure-4-1.png`}                  alt="Hardware schematic"                  caption="Figure 4.1: Hardware Schematic and Pin Mapping"                />              </div>            </Card>          </div>          <div className="mt-6 grid gap-4 md:grid-cols-5">            {[              { icon: <Cpu size={18} />, label: "Edge", value: "ESP32-CAM + sensors" },              { icon: <Wifi size={18} />, label: "Bridge", value: "ngrok TCP/HTTP tunnel" },              { icon: <Server size={18} />, label: "Orchestrator", value: "Node.js + soft sensors" },              { icon: <Server size={18} />, label: "AI Engine", value: "Flask (LSTM + CNN)" },              { icon: <Database size={18} />, label: "Database", value: "Supabase Postgres + Realtime" },            ].map((x) => (              <Card key={x.label} className="p-5">                <div className="flex items-center gap-2 text-slate-900">                  <span className="text-blue-600">{x.icon}</span>                  <div className="text-sm font-extrabold">{x.label}</div>                </div>                <div className="mt-2 text-sm text-slate-600">{x.value}</div>              </Card>            ))}          </div>          <div className="mt-6 rounded-3xl border border-slate-200 bg-gradient-to-br from-emerald-50 to-white p-6 shadow-sm">            <div className="flex items-center gap-2">              <span className="text-emerald-700">                <Zap size={18} />              </span>              <div className="text-sm font-extrabold">Automation Feedback Loop</div>            </div>            <p className="mt-2 text-sm text-slate-600 md:text-base">              Node.js evaluates risk level (High/Medium/Low) from predictions/diagnosis and sends trigger commands back to the              relay module to activate aerators or pumps—completing the closed-loop cycle with fail-safe behavior.            </p>          </div>        </Section>      </PageTransition>    </Layout>  );}