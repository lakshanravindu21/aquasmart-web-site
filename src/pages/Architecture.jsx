import React, { useState, useEffect, useRef } from "react";import Layout from "../components/Layout.jsx";import PageTransition from "../components/PageTransition.jsx";import Section, { Card, Badge } from "../components/Section.jsx";import ImageFrame from "../components/ImageFrame.jsx";import {  Server,  Wifi,  Cpu,  Database,  Zap,  Layers,  Network,  ShieldCheck,  HardDrive,  GitMerge,  Binary,  Activity,  ArrowRight,  Layers3,  Lightbulb,  Radio,  FlaskConical,  Globe,  MonitorSmartphone,} from "lucide-react";/* -- floating particle canvas ----------- */function ArchParticleCanvas() {  const ref = useRef(null);  useEffect(() => {    const canvas = ref.current;    if (!canvas) return;    const ctx = canvas.getContext("2d");    let raf;    const resize = () => {      canvas.width = canvas.offsetWidth;      canvas.height = canvas.offsetHeight;    };    resize();    window.addEventListener("resize", resize);    const COUNT = 55;    const pts = Array.from({ length: COUNT }, () => ({      x: Math.random() * canvas.width,      y: Math.random() * canvas.height,      r: Math.random() * 2.8 + 0.8,      vx: (Math.random() - 0.5) * 0.4,      vy: (Math.random() - 0.5) * 0.4,      a: Math.random() * 0.6 + 0.2,    }));    const draw = () => {      ctx.clearRect(0, 0, canvas.width, canvas.height);      for (let i = 0; i < pts.length; i++) {        for (let j = i + 1; j < pts.length; j++) {          const dx = pts[i].x - pts[j].x;          const dy = pts[i].y - pts[j].y;          const dist = Math.sqrt(dx * dx + dy * dy);          if (dist < 160) {            ctx.beginPath();            ctx.strokeStyle = `rgba(59, 130, 246, ${0.22 * (1 - dist / 160)})`;            ctx.lineWidth = 1.0;            ctx.moveTo(pts[i].x, pts[i].y);            ctx.lineTo(pts[j].x, pts[j].y);            ctx.stroke();          }        }      }      pts.forEach((p) => {        ctx.beginPath();        ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);        ctx.fillStyle = `rgba(96, 165, 250, ${p.a * 0.7})`;        ctx.fill();        p.x += p.vx;        p.y += p.vy;        if (p.x < 0 || p.x > canvas.width) p.vx *= -1;        if (p.y < 0 || p.y > canvas.height) p.vy *= -1;      });      raf = requestAnimationFrame(draw);    };    draw();    return () => {      cancelAnimationFrame(raf);      window.removeEventListener("resize", resize);    };  }, []);  return <canvas ref={ref} className="absolute inset-0 w-full h-full" />;}/* -- tech stack data ----------- */const techStack = [  {    icon: Cpu,    title: "ESP32-CAM + Hybrid Sensor Array",    subtitle: "Perception Layer",    desc: "Captures 10s high-frequency water telemetry and visual pathology frames at the pond edge.",    color: "#0891b2",    lightBg: "#ecfeff",    borderColor: "#a5f3fc",    num: "01",    subFeatures: [      { icon: Radio, label: "10-Second Polling Cycle" },      { icon: Cpu, label: "GPIO Multi-Sensor Fusion" },      { icon: Zap, label: "Opto-Isolated Relay Output" },    ],  },  {    icon: Wifi,    title: "ngrok Secure TCP/HTTP Tunnel",    subtitle: "Transmission Layer",    desc: "Bridges local edge nodes to the cloud infrastructure without requiring static IPs or port forwarding.",    color: "#7c3aed",    lightBg: "#f5f3ff",    borderColor: "#ddd6fe",    num: "02",    subFeatures: [      { icon: Globe, label: "Dynamic URL Bridging" },      { icon: ShieldCheck, label: "Encrypted TCP Relay" },      { icon: Wifi, label: "Zero Static IP Requirement" },    ],  },  {    icon: Server,    title: "Node.js + Soft-Sensor Logic",    subtitle: "System Orchestrator",    desc: "Handles data ingestion, mathematical DO/NH₃ derivation, risk evaluation, and actuator triggers.",    color: "#059669",    lightBg: "#ecfdf5",    borderColor: "#a7f3d0",    num: "03",    subFeatures: [      { icon: GitMerge, label: "Dual-Backend Bridge" },      { icon: Activity, label: "Risk Index Evaluation" },      { icon: Zap, label: "750ms Actuation Response" },    ],  },  {    icon: Binary,    title: "Flask — LSTM & MobileNetV2",    subtitle: "AI Inference Engine",    desc: "Isolated Python microservice handling resource-intensive deep learning inference, decoupled from the main backend.",    color: "#ea580c",    lightBg: "#fff7ed",    borderColor: "#fed7aa",    num: "04",    subFeatures: [      { icon: FlaskConical, label: "Python Microservice" },      { icon: Layers3, label: "LSTM + CNN Models" },      { icon: Binary, label: "Decoupled from Node.js" },    ],  },  {    icon: Database,    title: "Supabase + Realtime CDC",    subtitle: "Data Persistence",    desc: "Guarantees full data retention with change-data-capture WebSocket sync to the React dashboard.",    color: "#2563eb",    lightBg: "#eff6ff",    borderColor: "#bfdbfe",    num: "05",    subFeatures: [      { icon: Database, label: "PostgreSQL at the Core" },      { icon: MonitorSmartphone, label: "150ms WebSocket Sync" },      { icon: ShieldCheck, label: "100% Data Retention" },    ],  },];/* -- stack card (mirrors ModuleCard from Solution.jsx) ----------- */function StackCard({ p }) {  const [hovered, setHovered] = useState(false);  const Icon = p.icon;  return (    <div      className="bg-white rounded-3xl border flex flex-col overflow-hidden transition-all duration-500 h-full"      style={{        borderColor: hovered ? p.color : p.borderColor,        boxShadow: hovered          ? `0 20px 40px rgba(0,0,0,0.06), 0 0 0 1px ${p.color}22`          : "0 2px 10px rgba(0,0,0,0.02)",        transform: hovered ? "translateY(-5px)" : "translateY(0)",      }}      onMouseEnter={() => setHovered(true)}      onMouseLeave={() => setHovered(false)}    >      <div className="h-1.5 w-full" style={{ backgroundColor: p.color }} />      <div className="p-6 sm:p-8 flex flex-col flex-grow">        <div className="flex items-center justify-between mb-5">          <div            className="w-12 h-12 sm:w-14 sm:h-14 rounded-2xl flex items-center justify-center border transition-all duration-300"            style={{              backgroundColor: p.lightBg,              borderColor: hovered ? `${p.borderColor}aa` : "#e2e8f0",            }}          >            <Icon size={24} style={{ color: p.color }} />          </div>          <span            className="text-3xl sm:text-4xl font-black select-none transition-colors duration-300"            style={{ color: hovered ? p.color : "#e2e8f0" }}          >            {p.num}          </span>        </div>        <p          className="text-[10px] font-black uppercase tracking-[0.25em] mb-2"          style={{ color: p.color }}        >          {p.subtitle}        </p>        <h3 className="text-lg sm:text-xl font-extrabold text-slate-900 mb-3 leading-tight">          {p.title}        </h3>        <p className="text-sm sm:text-base text-slate-600 leading-relaxed flex-grow">          {p.desc}        </p>        <div className="mt-6 pt-5 border-t border-slate-100 grid grid-cols-1 gap-2">          {p.subFeatures.map((sub, i) => {            const SubIcon = sub.icon;            return (              <div                key={i}                className="flex items-center gap-3 px-3.5 py-2.5 rounded-xl transition-all duration-200"                style={{                  background: `linear-gradient(135deg, ${p.lightBg} 0%, white 100%)`,                  border: `1px solid ${p.borderColor}`,                }}              >                <div                  className="w-6 h-6 rounded-lg flex items-center justify-center flex-shrink-0"                  style={{ backgroundColor: `${p.color}18` }}                >                  <SubIcon size={13} style={{ color: p.color }} />                </div>                <span className="text-xs sm:text-sm font-semibold text-slate-700">                  {sub.label}                </span>              </div>            );          })}        </div>      </div>    </div>  );}/* -- figure card ----------- */function FigureCard({ accentColor, lightBg, borderColor, iconColor, iconBg, iconBorderColor, TitleIcon, title, subtitle, badge, src, alt, caption, captionBorderColor }) {  const [hovered, setHovered] = useState(false);  return (    <div      className="bg-white rounded-3xl border flex flex-col overflow-hidden transition-all duration-500"      style={{        borderColor: hovered ? accentColor : "#e2e8f0",        boxShadow: hovered          ? `0 20px 40px rgba(0,0,0,0.06), 0 0 0 1px ${accentColor}22`          : "0 2px 10px rgba(0,0,0,0.02)",        transform: hovered ? "translateY(-4px)" : "translateY(0)",      }}      onMouseEnter={() => setHovered(true)}      onMouseLeave={() => setHovered(false)}    >      <div className="h-1.5 w-full" style={{ backgroundColor: accentColor }} />      <div className="p-8 md:p-10 flex flex-col h-full">        <div className="flex items-center justify-between mb-8">          <div className="flex items-center gap-3">            <div              className="w-10 h-10 rounded-xl flex items-center justify-center border shadow-sm"              style={{ backgroundColor: iconBg, borderColor: iconBorderColor }}            >              <TitleIcon size={20} style={{ color: iconColor }} />            </div>            <div>              <h3 className="text-lg font-black text-slate-900 uppercase tracking-tight leading-none">                {title}              </h3>              <span className="text-[10px] font-bold text-slate-400 tracking-widest uppercase">                {subtitle}              </span>            </div>          </div>          <span            className="text-[10px] font-black uppercase tracking-widest px-3 py-1 rounded-full border"            style={{ backgroundColor: lightBg, color: accentColor, borderColor }}          >            {badge}          </span>        </div>        <div          className="rounded-2xl overflow-hidden border p-4 flex-grow transition-transform duration-500"          style={{            backgroundColor: "#f8fafc",            borderColor: "#f1f5f9",            transform: hovered ? "scale(1.01)" : "scale(1)",          }}        >          <ImageFrame src={src} alt={alt} className="w-full h-auto" />        </div>        <p          className="mt-6 text-xs text-slate-500 leading-relaxed font-medium italic pl-4"          style={{ borderLeft: `2px solid ${captionBorderColor}` }}        >          {caption}        </p>      </div>    </div>  );}export default function Architecture() {  const base = import.meta.env.BASE_URL;  return (    <Layout>      <PageTransition>        <div className="bg-slate-50 min-h-screen">          <div className="mx-auto w-full max-w-[1400px] px-4 sm:px-6 lg:px-12 py-10 md:py-16 lg:py-20">            {/* ── HEADER ─────────────────────────────────────── */}            <div              className="relative mb-12 md:mb-16 overflow-hidden rounded-2xl sm:rounded-3xl border border-slate-200 shadow-sm"              style={{ background: "linear-gradient(135deg, #ffffff 0%, #f8fafc 70%, #f1f5f9 100%)" }}            >              <div                className="absolute left-0 top-0 bottom-0 w-1 sm:w-1.5 rounded-l-2xl sm:rounded-l-3xl"                style={{ background: "linear-gradient(180deg, #0891b2 0%, #2563eb 50%, #7c3aed 100%)" }}              />              <div                className="absolute -top-16 -right-16 w-64 h-64 rounded-full pointer-events-none"                style={{ background: "radial-gradient(circle, rgba(59,130,246,0.12) 0%, transparent 70%)" }}              />              <div                className="absolute -bottom-12 left-1/3 w-48 h-48 rounded-full pointer-events-none"                style={{ background: "radial-gradient(circle, rgba(8,145,178,0.08) 0%, transparent 70%)" }}              />              <ArchParticleCanvas />              <div className="relative z-10 px-6 sm:px-10 md:px-14 lg:px-16 py-8 sm:py-10 md:py-11">                <div className="inline-flex items-center gap-2 px-3 sm:px-4 py-1.5 rounded-full mb-6 sm:mb-8 bg-white border border-slate-200 shadow-inner">                  <Network size={12} className="text-blue-500" />                  <span className="text-slate-500 text-[10px] font-black uppercase tracking-[0.3em]">                    System Design & Integration                  </span>                </div>                <div className="grid grid-cols-1 md:grid-cols-2 gap-8 md:gap-12 lg:gap-16 items-center">                  <div>                    <h1                      className="font-black text-slate-950 leading-tight"                      style={{ fontSize: "clamp(1.75rem, 5vw, 3.5rem)", letterSpacing: "-0.03em" }}                    >                      Multi-Tier{" "}                      <span className="text-blue-600">Architecture</span>{" "}                      & Dual-Backend.                    </h1>                  </div>                  <div className="flex flex-col gap-5">                    <p className="text-slate-600 text-sm sm:text-base md:text-lg leading-relaxed">                      AquaSmart utilizes a decentralized IoT ecosystem designed for                      distributed intelligence — isolating edge data acquisition from                      cloud reasoning to achieve sub-second automated responses.                    </p>                    <div className="flex flex-wrap gap-2">                      {[                        { label: "ESP32 Edge Layer",      bg: "#ecfeff", border: "#a5f3fc", color: "#0891b2" },                        { label: "Node.js Orchestrator",  bg: "#ecfdf5", border: "#a7f3d0", color: "#059669" },                        { label: "Flask AI Microservice", bg: "#fff7ed", border: "#fed7aa", color: "#ea580c" },                        { label: "Supabase Realtime DB",  bg: "#eff6ff", border: "#bfdbfe", color: "#2563eb" },                      ].map((chip, i) => (                        <span                          key={i}                          className="text-xs font-bold px-3 py-1.5 rounded-full"                          style={{ backgroundColor: chip.bg, border: `1px solid ${chip.border}`, color: chip.color }}                        >                          {chip.label}                        </span>                      ))}                    </div>                  </div>                </div>              </div>            </div>            {/* ── ARCHITECTURE DIAGRAMS DIVIDER ──────────────── */}            <div className="flex items-center gap-3 mb-8 md:mb-10">              <div className="h-px flex-1 bg-slate-200" />              <span className="text-[10px] sm:text-xs font-semibold uppercase tracking-widest text-slate-400 px-2 text-center">                Architecture & Hardware Diagrams              </span>              <div className="h-px flex-1 bg-slate-200" />            </div>            {/* ── ARCHITECTURE DIAGRAMS ───────────────────────── */}            <div className="grid gap-6 lg:gap-8 lg:grid-cols-2 mb-10 md:mb-14">              <FigureCard                accentColor="#2563eb"                lightBg="#eff6ff"                borderColor="#bfdbfe"                iconColor="#2563eb"                iconBg="#eff6ff"                iconBorderColor="#bfdbfe"                TitleIcon={Layers}                title="The Big Picture"                subtitle="Multi-Tier Data Flow"                badge="Figure 3.1"                src={`${base}assets/figure-3-1.png`}                alt="System architecture"                captionBorderColor="#bfdbfe"                caption="Architecture flow: Perception (ESP32) → Transmission (ngrok) → dual Processing (Node/Flask) → persistent Application (Supabase/React)."              />              <FigureCard                accentColor="#7c3aed"                lightBg="#f5f3ff"                borderColor="#ddd6fe"                iconColor="#7c3aed"                iconBg="#f5f3ff"                iconBorderColor="#ddd6fe"                TitleIcon={HardDrive}                title="Hardware Layer"                subtitle="Node Pin Mapping"                badge="Figure 4.1"                src={`${base}assets/figure-4-1.png`}                alt="Hardware schematic"                captionBorderColor="#ddd6fe"                caption="ESP32-CAM schematic showing opto-isolated relay triggers on GPIO 12/13 and multi-sensor integration on pins 32 and 4."              />            </div>            {/* ── TECH STACK DIVIDER ─────────────────────────── */}            <div className="flex items-center gap-3 mb-8 md:mb-10">              <div className="h-px flex-1 bg-slate-200" />              <span className="text-[10px] sm:text-xs font-semibold uppercase tracking-widest text-slate-400 px-2 text-center">                5 Integrated Technology Layers              </span>              <div className="h-px flex-1 bg-slate-200" />            </div>            {/* ── TECH STACK GRID ────────────────────────────── */}            <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5 sm:gap-6 lg:gap-8 mb-10 md:mb-14">              {techStack.map((m, idx) => (                <StackCard key={idx} p={m} />              ))}            </div>            {/* ── AUTOMATION HIGHLIGHT ───────────────────────── */}            <div className="relative overflow-hidden rounded-2xl sm:rounded-3xl border border-slate-200 bg-white shadow-lg">              {/* Top gradient accent */}              <div                className="h-1.5 w-full"                style={{ background: "linear-gradient(90deg, #0891b2 0%, #2563eb 40%, #059669 100%)" }}              />              {/* Decorative background */}              <div className="absolute inset-0 pointer-events-none overflow-hidden">                <div                  className="absolute -top-24 -right-24 w-96 h-96 rounded-full"                  style={{ background: "radial-gradient(circle, rgba(5,150,105,0.06) 0%, transparent 70%)" }}                />                <div                  className="absolute -bottom-20 -left-20 w-80 h-80 rounded-full"                  style={{ background: "radial-gradient(circle, rgba(59,130,246,0.05) 0%, transparent 70%)" }}                />                <div className="absolute top-0 right-0 p-8 opacity-[0.03]">                  <ShieldCheck size={220} className="text-slate-900" />                </div>              </div>              <div className="relative z-10 p-8 sm:p-10 md:p-12 lg:p-16">                <p className="text-[10px] font-black uppercase tracking-[0.3em] text-slate-400 mb-2">                  Closed-Loop Stability                </p>                <h2 className="text-xl sm:text-2xl md:text-3xl font-black text-slate-900 mb-10 sm:mb-12 tracking-tight">                  Autonomous Intervention Bridge                </h2>                {/* ── Data flow pipeline ── */}                <div className="mb-10 sm:mb-12">                  <p className="text-[10px] font-black uppercase tracking-[0.25em] text-slate-400 mb-5">                    Automated Response Sequence                  </p>                  <div className="flex items-stretch gap-2 sm:gap-3">                    {[                      { icon: Cpu,        label: "Edge Sensing",    sublabel: "10s telemetry",        color: "#0891b2", lightBg: "#ecfeff", borderColor: "#a5f3fc" },                      { icon: Wifi,       label: "ngrok Tunnel",    sublabel: "Encrypted relay",      color: "#7c3aed", lightBg: "#f5f3ff", borderColor: "#ddd6fe" },                      { icon: Binary,     label: "AI Inference",    sublabel: "Risk score output",    color: "#ea580c", lightBg: "#fff7ed", borderColor: "#fed7aa" },                      { icon: Server,     label: "Node.js Logic",   sublabel: "Threshold evaluation", color: "#2563eb", lightBg: "#eff6ff", borderColor: "#bfdbfe" },                      { icon: Zap,        label: "Relay Trigger",   sublabel: "Sub-750ms response",   color: "#059669", lightBg: "#ecfdf5", borderColor: "#a7f3d0" },                    ].map((step, i, arr) => {                      const StepIcon = step.icon;                      const [h, setH] = useState(false);                      return (                        <div key={i} className="flex items-center gap-2 sm:gap-3 flex-1 min-w-0">                          <div className="flex flex-col items-center flex-1 min-w-0">                            <div                              className="w-full rounded-2xl border p-3 sm:p-4 flex flex-col items-center gap-2 text-center transition-all duration-300 cursor-default"                              style={{                                backgroundColor: h ? step.lightBg : "#ffffff",                                borderColor: h ? step.color : step.borderColor,                                boxShadow: h ? `0 8px 24px ${step.color}18` : "0 2px 8px rgba(0,0,0,0.03)",                                transform: h ? "translateY(-3px)" : "translateY(0)",                              }}                              onMouseEnter={() => setH(true)}                              onMouseLeave={() => setH(false)}                            >                              <div                                className="w-9 h-9 rounded-xl flex items-center justify-center"                                style={{ backgroundColor: `${step.color}15` }}                              >                                <StepIcon size={18} style={{ color: step.color }} />                              </div>                              <p className="text-[10px] sm:text-xs font-black text-slate-800 uppercase tracking-tight leading-tight">{step.label}</p>                              <p className="text-[9px] sm:text-[10px] font-medium text-slate-400 leading-tight hidden sm:block">{step.sublabel}</p>                            </div>                          </div>                          {i < arr.length - 1 && (                            <div className="flex-shrink-0 text-slate-300">                              <ArrowRight size={14} />                            </div>                          )}                        </div>                      );                    })}                  </div>                </div>                {/* ── Bottom two-column ── */}                <div className="grid grid-cols-1 md:grid-cols-2 gap-6 sm:gap-8">                  {/* Left: narrative */}                  <div                    className="rounded-2xl border p-6 sm:p-8 flex flex-col justify-between"                    style={{ backgroundColor: "#f8fafc", borderColor: "#e2e8f0" }}                  >                    <div>                      <p className="text-[10px] font-black uppercase tracking-[0.25em] text-slate-400 mb-3">                        System Rationale                      </p>                      <p className="text-sm sm:text-base text-slate-700 leading-relaxed font-medium">                        Node.js orchestrates the feedback loop by continuously evaluating risk indices                        from both the LSTM forecasting engine and the MobileNetV2 classifier. When a                        biological or chemical threat is detected, the system issues an immediate command                        through the ngrok tunnel to toggle 230V aerators and pumps — without any human                        intervention in the critical response window.                      </p>                    </div>                    <div className="mt-6 pt-5 border-t border-slate-200">                      <p className="text-[10px] font-black uppercase tracking-[0.25em] text-slate-400 mb-3">                        Key Design Principle                      </p>                      <p className="text-sm font-semibold text-slate-600 leading-relaxed">                        By decoupling the Flask AI microservice from Node.js orchestration, each layer scales independently — preventing inference latency from blocking the sub-750ms actuation path.                      </p>                    </div>                  </div>                  {/* Right: architecture pillars */}                  <div className="flex flex-col gap-3">                    {[                      {                        icon: Network,                        color: "#0891b2",                        lightBg: "#ecfeff",                        borderColor: "#a5f3fc",                        label: "Distributed Intelligence",                        desc: "Edge nodes handle sensing and actuation locally; cloud handles reasoning — preventing single-point failure.",                      },                      {                        icon: Binary,                        color: "#ea580c",                        lightBg: "#fff7ed",                        borderColor: "#fed7aa",                        label: "Dual-Backend Isolation",                        desc: "Flask microservice is fully isolated from the Node.js core — heavy deep learning inference never blocks real-time data flow.",                      },                      {                        icon: ShieldCheck,                        color: "#059669",                        lightBg: "#ecfdf5",                        borderColor: "#a7f3d0",                        label: "Sub-750ms Response Path",                        desc: "The actuation chain from risk detection to relay toggle is optimized to complete within 750ms, protecting pond stability.",                      },                    ].map((pillar, i) => {                      const PillarIcon = pillar.icon;                      return (                        <div                          key={i}                          className="flex gap-4 rounded-2xl border p-4 sm:p-5"                          style={{                            background: `linear-gradient(135deg, ${pillar.lightBg} 0%, white 100%)`,                            borderColor: pillar.borderColor,                          }}                        >                          <div                            className="w-9 h-9 rounded-xl flex items-center justify-center flex-shrink-0 mt-0.5"                            style={{ backgroundColor: `${pillar.color}18` }}                          >                            <PillarIcon size={17} style={{ color: pillar.color }} />                          </div>                          <div>                            <p className="text-xs font-black text-slate-800 uppercase tracking-tight mb-1">                              {pillar.label}                            </p>                            <p className="text-xs sm:text-sm text-slate-500 font-medium leading-relaxed">                              {pillar.desc}                            </p>                          </div>                        </div>                      );                    })}                  </div>                </div>              </div>            </div>          </div>        </div>      </PageTransition>    </Layout>  );}