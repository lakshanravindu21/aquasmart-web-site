import React, { useState, useEffect, useRef } from "react";import Layout from "../components/Layout.jsx";import PageTransition from "../components/PageTransition.jsx";import Section, { Card, Badge } from "../components/Section.jsx";import {  Radar,  BrainCircuit,  Zap,  FileText,  Unplug,  Droplets,  Camera,  Layers3,  Lightbulb,  Server,  Microscope,  Database,  Smartphone,  CheckCircle2,  AlertTriangle,  TrendingDown,  Target,} from "lucide-react";/* -- floating particle canvas ----------- */function SolutionParticleCanvas() {  const ref = useRef(null);  useEffect(() => {    const canvas = ref.current;    if (!canvas) return;    const ctx = canvas.getContext("2d");    let raf;    const resize = () => {      canvas.width = canvas.offsetWidth;      canvas.height = canvas.offsetHeight;    };    resize();    window.addEventListener("resize", resize);    const COUNT = 55;    const pts = Array.from({ length: COUNT }, () => ({      x: Math.random() * canvas.width,      y: Math.random() * canvas.height,      r: Math.random() * 2.8 + 0.8,      vx: (Math.random() - 0.5) * 0.4,      vy: (Math.random() - 0.5) * 0.4,      a: Math.random() * 0.6 + 0.2,    }));    const draw = () => {      ctx.clearRect(0, 0, canvas.width, canvas.height);      for (let i = 0; i < pts.length; i++) {        for (let j = i + 1; j < pts.length; j++) {          const dx = pts[i].x - pts[j].x;          const dy = pts[i].y - pts[j].y;          const dist = Math.sqrt(dx * dx + dy * dy);          if (dist < 160) {            ctx.beginPath();            ctx.strokeStyle = `rgba(8, 145, 178, ${0.22 * (1 - dist / 160)})`;            ctx.lineWidth = 1.0;            ctx.moveTo(pts[i].x, pts[i].y);            ctx.lineTo(pts[j].x, pts[j].y);            ctx.stroke();          }        }      }      pts.forEach((p) => {        ctx.beginPath();        ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);        ctx.fillStyle = `rgba(6, 182, 212, ${p.a * 0.7})`;        ctx.fill();        p.x += p.vx;        p.y += p.vy;        if (p.x < 0 || p.x > canvas.width) p.vx *= -1;        if (p.y < 0 || p.y > canvas.height) p.vy *= -1;      });      raf = requestAnimationFrame(draw);    };    draw();    return () => {      cancelAnimationFrame(raf);      window.removeEventListener("resize", resize);    };  }, []);  return <canvas ref={ref} className="absolute inset-0 w-full h-full" />;}/* -- solution modules data ----------- */const modules = [  {    icon: Radar,    title: "10s High-Freq Telemetry",    subtitle: "IoT Data Ingestion Layer",    desc: "A redundant array of physical and virtual (soft) sensors deployed at the pond edge captures water parameters every 10 seconds. This persistent stream eliminates temporal monitoring blind spots.",    color: "#0891b2",    lightBg: "#ecfeff",    borderColor: "#a5f3fc",    num: "01",    subFeatures: [      { icon: Droplets, label: "pH, Temp, Turbidity, Level" },      { icon: Unplug, label: "Soft-Sensor DO (Derivation)" },      { icon: Database, label: "Soft-Sensor NH₃ (Derivation)" },    ],  },  {    icon: Microscope,    title: "ESP32-CAM Pathology Scan",    subtitle: "Site-Level Vision Layer",    desc: "Integrated ESP32-CAM nodes capture high-resolution imagery of shrimp specimens at regular intervals or on demand. Visual data is streamed to the AI layer for instant pathological analysis.",    color: "#4f46e5",    lightBg: "#eef2ff",    borderColor: "#c7d2fe",    num: "02",    subFeatures: [      { icon: Camera, label: "Scheduled Image Capture" },      { icon: Smartphone, label: "On-demand manual trigger" },      { icon: Database, label: "Visual Data Streaming" },    ],  },  {    icon: BrainCircuit,    title: "LSTM & CNN Reasoning",    subtitle: "AI Intelligence Layer",    desc: "Cloud-hosted Deep Learning models process heterogeneous data: LSTM forecasts water quality 24 hours in advance, while MobileNetV2 diagnoses WSSV and Black Gill symptoms from edge images with high confidence scores.",    color: "#7c3aed",    lightBg: "#f5f3ff",    borderColor: "#ddd6fe",    num: "03",    subFeatures: [      { icon: TrendingDown, label: "24h Trend Forecasting" },      { icon: Microscope, label: "Disease Classification" },      { icon: Server, label: "Dual-Backend Bridge (Flask+Node)" },    ],  },  {    icon: Zap,    title: "Closed-Loop Response",    subtitle: "Actuation & Automation Layer",    desc: "Node.js risk evaluator automatically triggers edge actuators (aerators, pumps) via IoT relays within 750ms of threshold breach, utilizing predictive AI risk scores for stabilization.",    color: "#059669",    lightBg: "#ecfdf5",    borderColor: "#a7f3d0",    num: "04",    subFeatures: [      { icon: Zap, label: "750ms Logic Latency" },      { icon: AlertTriangle, label: "Predictive Triggering" },      { icon: Smartphone, label: "Manual Override Control" },    ],  },  {    icon: Smartphone,    title: "Mobile-First Dashboard",    subtitle: "Realtime Visualization Layer",    desc: "A centralized React dashboard provides instantaneous visualization of raw telemetry, LSTM predictive charts, and CNN diagnostic results with confidence scores, synchronizing in under 150ms.",    color: "#ea580c",    lightBg: "#fff7ed",    borderColor: "#fed7aa",    num: "05",    subFeatures: [      { icon: CheckCircle2, label: "150ms Realtime Sync" },      { icon: TrendingDown, label: "Forecasting Chart Integration" },      { icon: Camera, label: "Diagnostic Image Viewer" },    ],  },  {    icon: FileText,    title: "Automated Diagnostic Reports",    subtitle: "Traceability & Reporting Layer",    desc: "Every pathological scan generates an automated diagnostic PDF report. Capturing detected conditions, confidence scores, and environmental parameters at the time of detection for audit trails.",    color: "#0f172a",    lightBg: "#f1f5f9",    borderColor: "#e2e8f0",    num: "06",    subFeatures: [      { icon: CheckCircle2, label: "Scan Timestamp Logging" },      { icon: FileText, label: "Per-Scan PDF Generation" },      { icon: Server, label: "Confidence Score Archival" },    ],  },];/* -- module card ----------- */function ModuleCard({ p }) {  const [hovered, setHovered] = useState(false);  const Icon = p.icon;  return (    <div      className="bg-white rounded-3xl border flex flex-col overflow-hidden transition-all duration-500 h-full"      style={{        borderColor: hovered ? p.color : p.borderColor,        boxShadow: hovered          ? `0 20px 40px rgba(0,0,0,0.06), 0 0 0 1px ${p.color}22`          : "0 2px 10px rgba(0,0,0,0.02)",        transform: hovered ? "translateY(-5px)" : "translateY(0)",      }}      onMouseEnter={() => setHovered(true)}      onMouseLeave={() => setHovered(false)}    >      <div className="h-1.5 w-full" style={{ backgroundColor: p.color }} />      <div className="p-6 sm:p-8 flex flex-col flex-grow">        {/* Icon + Number */}        <div className="flex items-center justify-between mb-5">          <div            className="w-12 h-12 sm:w-14 sm:h-14 rounded-2xl flex items-center justify-center border transition-all duration-300"            style={{              backgroundColor: p.lightBg,              borderColor: hovered ? `${p.borderColor}aa` : "#e2e8f0",            }}          >            <Icon size={24} style={{ color: p.color }} />          </div>          <span            className="text-3xl sm:text-4xl font-black select-none transition-colors duration-300"            style={{ color: hovered ? p.color : "#e2e8f0" }}          >            {p.num}          </span>        </div>        {/* Subtitle */}        <p          className="text-[10px] font-black uppercase tracking-[0.25em] mb-2"          style={{ color: p.color }}        >          {p.subtitle}        </p>        {/* Title */}        <h3 className="text-lg sm:text-xl font-extrabold text-slate-900 mb-3 leading-tight">          {p.title}        </h3>        {/* Description */}        <p className="text-sm sm:text-base text-slate-600 leading-relaxed flex-grow">          {p.desc}        </p>        {/* Sub-features */}        <div className="mt-6 pt-5 border-t border-slate-100 grid grid-cols-1 gap-2">          {p.subFeatures.map((sub, i) => {            const SubIcon = sub.icon;            return (              <div                key={i}                className="flex items-center gap-3 px-3.5 py-2.5 rounded-xl transition-all duration-200"                style={{                  background: `linear-gradient(135deg, ${p.lightBg} 0%, white 100%)`,                  border: `1px solid ${p.borderColor}`,                }}              >                <div                  className="w-6 h-6 rounded-lg flex items-center justify-center flex-shrink-0"                  style={{ backgroundColor: `${p.color}18` }}                >                  <SubIcon size={13} style={{ color: p.color }} />                </div>                <span className="text-xs sm:text-sm font-semibold text-slate-700">{sub.label}</span>              </div>            );          })}        </div>      </div>    </div>  );}export default function Solution() {  return (    <Layout>      <PageTransition>        <div className="bg-slate-50 min-h-screen">          <div className="mx-auto w-full max-w-[1400px] px-4 sm:px-6 lg:px-12 py-10 md:py-16 lg:py-20">            {/* ── HEADER ─────────────────────────────────────── */}            <div              className="relative mb-12 md:mb-16 overflow-hidden rounded-2xl sm:rounded-3xl border border-slate-200 shadow-sm"              style={{ background: "linear-gradient(135deg, #ffffff 0%, #f8fafc 70%, #f1f5f9 100%)" }}            >              {/* Left accent bar */}              <div                className="absolute left-0 top-0 bottom-0 w-1 sm:w-1.5 rounded-l-2xl sm:rounded-l-3xl"                style={{ background: "linear-gradient(180deg, #0891b2 0%, #7c3aed 50%, #059669 100%)" }}              />              {/* Glow blobs */}              <div className="absolute -top-16 -right-16 w-64 h-64 rounded-full pointer-events-none"                style={{ background: "radial-gradient(circle, rgba(6,182,212,0.12) 0%, transparent 70%)" }} />              <div className="absolute -bottom-12 left-1/3 w-48 h-48 rounded-full pointer-events-none"                style={{ background: "radial-gradient(circle, rgba(124,58,237,0.08) 0%, transparent 70%)" }} />              {/* Particle animation */}              <SolutionParticleCanvas />              {/* Content */}              <div className="relative z-10 px-6 sm:px-10 md:px-14 lg:px-16 py-8 sm:py-10 md:py-11">                {/* Badge */}                <div className="inline-flex items-center gap-2 px-3 sm:px-4 py-1.5 rounded-full mb-6 sm:mb-8 bg-white border border-slate-200 shadow-inner">                  <Lightbulb size={12} className="text-cyan-500" />                  <span className="text-slate-500 text-[10px] font-black uppercase tracking-[0.3em]">                    Proposed Methodology                  </span>                </div>                {/* Two-column layout — stacks on mobile */}                <div className="grid grid-cols-1 md:grid-cols-2 gap-8 md:gap-12 lg:gap-16 items-center">                  {/* Left: Heading */}                  <div>                    <h1                      className="font-black text-slate-950 leading-tight"                      style={{                        fontSize: "clamp(1.75rem, 5vw, 3.5rem)",                        letterSpacing: "-0.03em",                      }}                    >                      The{" "}                      <span className="text-cyan-600">AquaSmart</span>{" "}                      Integrated AIoT Ecosystem.                    </h1>                  </div>                  {/* Right: Description + badges */}                  <div className="flex flex-col gap-5">                    <p className="text-slate-600 text-sm sm:text-base md:text-lg leading-relaxed">                      To move beyond reactive management, AquaSmart deploys a                      multi-tier solution architecture. We seamlessly integrate                      high-frequency edge sensing, cloud-based deep learning                      reasoning, automated closed-loop interventions, and                      comprehensive data visualization.                    </p>                    <div className="flex flex-wrap gap-2">                      {[                        { label: "AIoT Architecture", bg: "#ecfeff", border: "#a5f3fc", color: "#0891b2" },                        { label: "LSTM & CNN Reasoning", bg: "#eef2ff", border: "#c7d2fe", color: "#4f46e5" },                        { label: "Closed-Loop Stabilization", bg: "#ecfdf5", border: "#a7f3d0", color: "#059669" },                      ].map((chip, i) => (                        <span                          key={i}                          className="text-xs font-bold px-3 py-1.5 rounded-full"                          style={{                            backgroundColor: chip.bg,                            border: `1px solid ${chip.border}`,                            color: chip.color,                          }}                        >                          {chip.label}                        </span>                      ))}                    </div>                  </div>                </div>              </div>            </div>            {/* ── GRID DIVIDER ───────────────────────────────── */}            <div className="flex items-center gap-3 mb-8 md:mb-10">              <div className="h-px flex-1 bg-slate-200" />              <span className="text-[10px] sm:text-xs font-semibold uppercase tracking-widest text-slate-400 px-2 text-center">                6 Integrated Solution Modules              </span>              <div className="h-px flex-1 bg-slate-200" />            </div>            {/* ── MODULES GRID ───────────────────────────────── */}            <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5 sm:gap-6 lg:gap-8 mb-10 md:mb-14">              {modules.map((m, idx) => (                <ModuleCard key={idx} p={m} />              ))}            </div>            {/* ── SOLUTION SUMMARY ───────────────────────────── */}            <div className="relative overflow-hidden rounded-2xl sm:rounded-3xl border border-slate-200 bg-white p-8 sm:p-10 md:p-12 lg:p-16 shadow-lg">              {/* Decorative icon */}              <div className="absolute top-0 right-0 p-6 sm:p-10 opacity-[0.04] pointer-events-none">                <Target size={200} className="text-slate-900" />              </div>              <div className="relative z-10">                {/* Label */}                <p className="text-[10px] font-black uppercase tracking-[0.3em] text-slate-400 mb-2">                  Strategic Impact                </p>                <h2 className="text-xl sm:text-2xl md:text-3xl font-black text-slate-900 mb-8 tracking-tight">                  Research Outcome                </h2>                <div className="grid grid-cols-1 md:grid-cols-5 gap-8 items-start md:items-center">                  {/* Left: description */}                  <div className="md:col-span-3">                    <p className="text-base sm:text-lg md:text-xl text-slate-600 leading-relaxed font-medium">                      By deploying this decentralized yet unified AIoT framework,                      AquaSmart bridges the "Reactive Gap." Transitioning prawn                      farming from human-led manual intervention to AI-led                      proactive stabilization, securing operational continuity                      and maximizing crop yield for small-scale coastal farmers.                    </p>                  </div>                  {/* Right: outcome bullets */}                  <div className="md:col-span-2 flex flex-col gap-4 bg-slate-50 border border-slate-100 p-6 sm:p-8 rounded-2xl">                    {[                      { icon: Zap, color: "#059669", label: "Goal", value: "Proactive Mortality Reduction" },                      { icon: Layers3, color: "#4f46e5", label: "Output", value: "Autonomous Operational Continuity" },                      { icon: Smartphone, color: "#0891b2", label: "Platform", value: "Democratized Precision Aquaculture" },                    ].map((item, i) => {                      const Icon = item.icon;                      return (                        <div key={i} className="flex items-start gap-3">                          <div                            className="mt-0.5 w-8 h-8 rounded-xl flex items-center justify-center flex-shrink-0"                            style={{ backgroundColor: `${item.color}15` }}                          >                            <Icon size={16} style={{ color: item.color }} />                          </div>                          <p className="text-sm sm:text-base font-semibold text-slate-700 leading-snug">                            <span className="text-slate-400 font-medium">{item.label}: </span>                            {item.value}                          </p>                        </div>                      );                    })}                  </div>                </div>              </div>            </div>          </div>        </div>      </PageTransition>    </Layout>  );}