import React from "react";import Layout from "../components/Layout.jsx";import PageTransition from "../components/PageTransition.jsx";import Section, { Card, Badge } from "../components/Section.jsx";import ImageFrame from "../components/ImageFrame.jsx";import { Monitor, Eye, FileText } from "lucide-react";export default function UIScreens() {  const base = import.meta.env.BASE_URL;  const screens = [    {      fig: "5.4",      title: "AquaSmart Dashboard Overview",      desc: "Realtime 6-parameter telemetry + risk classification via realtime updates.",      src: `${base}assets/figure-5-4.png`,      icon: <Monitor size={18} />,    },    {      fig: "5.5",      title: "Predictive Analytics Interface",      desc: "24-hour forecasting view + local biological advisories for decision support.",      src: `${base}assets/figure-5-5.png`,      icon: <Monitor size={18} />,    },    {      fig: "5.6",      title: "Live Computer Vision Module",      desc: "ESP32-CAM feed with detection overlay and confidence score.",      src: `${base}assets/figure-5-6.png`,      icon: <Eye size={18} />,    },    {      fig: "5.7",      title: "Disease Control & Report Center",      desc: "Upload images + generate downloadable PDF diagnostic reports.",      src: `${base}assets/figure-5-7.png`,      icon: <FileText size={18} />,    },  ];  return (    <Layout>      <PageTransition>        <Section          eyebrow="Frontend"          title="UI Modules & Screens"          subtitle="Mobile-first React dashboard designed for realtime monitoring, forecasting, diagnosis, and manual override control."        >          <div className="grid gap-6 md:grid-cols-2">            {screens.map((s) => (              <Card key={s.fig}>                <div className="flex items-start justify-between gap-3">                  <div className="flex items-center gap-2">                    <span className="text-blue-600">{s.icon}</span>                    <div>                      <div className="text-sm font-extrabold">{s.title}</div>                      <div className="mt-1 text-xs text-slate-500">{s.desc}</div>                    </div>                  </div>                  <Badge>Figure {s.fig}</Badge>                </div>                <div className="mt-4">                  <ImageFrame src={s.src} alt={s.title} caption={`Figure ${s.fig}: ${s.title}`} />                </div>              </Card>            ))}          </div>        </Section>      </PageTransition>    </Layout>  );}