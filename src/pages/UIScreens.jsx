import React, { useState, useEffect, useRef } from "react";import Layout from "../components/Layout.jsx";import PageTransition from "../components/PageTransition.jsx";import Section, { Card, Badge } from "../components/Section.jsx";import ImageFrame from "../components/ImageFrame.jsx";import {  Monitor,  Eye,  FileText,  LineChart,  ShieldCheck,  Zap,  TrendingUp,} from "lucide-react";/* ── particle canvas ─────────────────────────────────── */function UIParticleCanvas() {  const ref = useRef(null);  useEffect(() => {    const canvas = ref.current;    if (!canvas) return;    const ctx = canvas.getContext("2d");    let raf;    const resize = () => {      canvas.width = canvas.offsetWidth;      canvas.height = canvas.offsetHeight;    };    resize();    window.addEventListener("resize", resize);    const COUNT = 55;    const pts = Array.from({ length: COUNT }, () => ({      x: Math.random() * canvas.width,      y: Math.random() * canvas.height,      r: Math.random() * 2.8 + 0.8,      vx: (Math.random() - 0.5) * 0.4,      vy: (Math.random() - 0.5) * 0.4,      a: Math.random() * 0.6 + 0.2,    }));    const draw = () => {      ctx.clearRect(0, 0, canvas.width, canvas.height);      for (let i = 0; i < pts.length; i++) {        for (let j = i + 1; j < pts.length; j++) {          const dx = pts[i].x - pts[j].x;          const dy = pts[i].y - pts[j].y;          const dist = Math.sqrt(dx * dx + dy * dy);          if (dist < 160) {            ctx.beginPath();            ctx.strokeStyle = `rgba(37, 99, 235, ${0.22 * (1 - dist / 160)})`;            ctx.lineWidth = 1.0;            ctx.moveTo(pts[i].x, pts[i].y);            ctx.lineTo(pts[j].x, pts[j].y);            ctx.stroke();          }        }      }      pts.forEach((p) => {        ctx.beginPath();        ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);        ctx.fillStyle = `rgba(96, 165, 250, ${p.a * 0.7})`;        ctx.fill();        p.x += p.vx;        p.y += p.vy;        if (p.x < 0 || p.x > canvas.width) p.vx *= -1;        if (p.y < 0 || p.y > canvas.height) p.vy *= -1;      });      raf = requestAnimationFrame(draw);    };    draw();    return () => {      cancelAnimationFrame(raf);      window.removeEventListener("resize", resize);    };  }, []);  return <canvas ref={ref} className="absolute inset-0 w-full h-full" />;}/* ── screen card ─────────────────────────────────────── */function ScreenCard({ s }) {  const [hovered, setHovered] = useState(false);  const Icon = s.icon;  return (    <div      className="bg-white rounded-3xl border flex flex-col overflow-hidden transition-all duration-500"      style={{        borderColor: hovered ? s.color : s.borderColor,        boxShadow: hovered          ? `0 20px 40px rgba(0,0,0,0.08), 0 0 0 1px ${s.color}22`          : "0 2px 10px rgba(0,0,0,0.02)",        transform: hovered ? "translateY(-5px)" : "translateY(0)",      }}      onMouseEnter={() => setHovered(true)}      onMouseLeave={() => setHovered(false)}    >      {/* Color stripe */}      <div className="h-1.5 w-full" style={{ backgroundColor: s.color }} />      <div className="p-6 sm:p-8 flex flex-col flex-grow">        {/* Header row */}        <div className="flex items-start justify-between gap-3 mb-6">          <div className="flex items-center gap-3 min-w-0">            <div              className="w-11 h-11 rounded-2xl flex items-center justify-center border flex-shrink-0 transition-all duration-300"              style={{                backgroundColor: hovered ? s.color : s.lightBg,                borderColor: hovered ? s.color : s.borderColor,              }}            >              <Icon size={20} style={{ color: hovered ? "#ffffff" : s.color }} />            </div>            <div className="min-w-0">              <p                className="text-[10px] font-black uppercase tracking-[0.25em] mb-0.5"                style={{ color: s.color }}              >                {s.module}              </p>              <h3 className="text-base sm:text-lg font-extrabold text-slate-900 leading-tight">                {s.title}              </h3>            </div>          </div>          <span            className="text-[10px] font-black uppercase tracking-widest px-2.5 py-1 rounded-full border flex-shrink-0"            style={{ backgroundColor: s.lightBg, color: s.color, borderColor: s.borderColor }}          >            Fig {s.fig}          </span>        </div>        {/* Image */}        <div          className="rounded-2xl overflow-hidden border mb-5 transition-all duration-500"          style={{            backgroundColor: "#f8fafc",            borderColor: hovered ? s.borderColor : "#f1f5f9",            boxShadow: hovered ? `inset 0 0 0 2px ${s.color}18` : "none",          }}        >          <div className="p-3">            <ImageFrame              src={s.src}              alt={s.title}              className="w-full h-auto transition-transform duration-700"              style={{ transform: hovered ? "scale(1.02)" : "scale(1)" }}            />          </div>        </div>        {/* Description */}        <p className="text-sm text-slate-500 font-medium leading-relaxed flex-grow mb-5">          {s.desc}        </p>        {/* Footer */}        <div          className="flex items-center justify-between pt-4 border-t"          style={{ borderColor: hovered ? s.borderColor : "#f1f5f9" }}        >          <span className="text-[10px] font-bold uppercase tracking-widest text-slate-300">            AquaSmart v1.0          </span>          <span className="flex items-center gap-1.5 text-[10px] font-bold uppercase tracking-widest" style={{ color: "#059669" }}>            <div className="h-1.5 w-1.5 rounded-full bg-emerald-500 animate-pulse" />            Interface Validated          </span>        </div>      </div>    </div>  );}/* ── data ─────────────────────────────────────────────── */const screens = [  {    fig: "5.4",    module: "Telemetry Overview",    title: "Main Dashboard",    desc: "Real-time 6-parameter telemetry visualization with high-frequency sensor updates and live risk classification indicators.",    src: "figure-5-4.png",    icon: Monitor,    color: "#2563eb",    lightBg: "#eff6ff",    borderColor: "#bfdbfe",  },  {    fig: "5.5",    module: "Predictive Analytics",    title: "LSTM Forecasting Interface",    desc: "Proactive management hub displaying LSTM-driven 24-hour forecasting curves and biological advisories for decision support.",    src: "figure-5-5.png",    icon: LineChart,    color: "#7c3aed",    lightBg: "#f5f3ff",    borderColor: "#ddd6fe",  },  {    fig: "5.6",    module: "Computer Vision",    title: "Live Pathology Stream",    desc: "Real-time ESP32-CAM visual stream with pathological detection overlays and CNN-derived confidence scores rendered instantly.",    src: "figure-5-6.png",    icon: Eye,    color: "#0891b2",    lightBg: "#ecfeff",    borderColor: "#a5f3fc",  },  {    fig: "5.7",    module: "Diagnostic Records",    title: "Pathological Report Center",    desc: "Comprehensive diagnostic interface for historical scan review and automated PDF report generation with full audit trails.",    src: "figure-5-7.png",    icon: FileText,    color: "#ea580c",    lightBg: "#fff7ed",    borderColor: "#fed7aa",  },  {    fig: "5.8",    module: "Automation Control",    title: "System Control Center",    desc: "Interface for managing closed-loop feedback, featuring manual actuator overrides and real-time status of aerators and pumps.",    src: "figure-5-8.png",    icon: Zap,    color: "#059669",    lightBg: "#ecfdf5",    borderColor: "#a7f3d0",  },  {    fig: "5.9",    module: "Trend Analysis",    title: "Water Quality Forecasting",    desc: "Visualization of forecasted environmental trends derived from the LSTM model, trained on 17,000+ data points.",    src: "figure-5-9.png",    icon: TrendingUp,    color: "#dc2626",    lightBg: "#fef2f2",    borderColor: "#fecaca",  },];/* ── component ───────────────────────────────────────── */export default function UIScreens() {  const base = import.meta.env.BASE_URL;  return (    <Layout>      <PageTransition>        <div className="bg-slate-50 min-h-screen">          <div className="mx-auto w-full max-w-[1400px] px-4 sm:px-6 lg:px-12 py-10 md:py-16 lg:py-20">            {/* ── HEADER ──────────────────────────────────── */}            <div              className="relative mb-12 md:mb-16 overflow-hidden rounded-2xl sm:rounded-3xl border border-slate-200 shadow-sm"              style={{ background: "linear-gradient(135deg, #ffffff 0%, #f8fafc 70%, #f1f5f9 100%)" }}            >              <div                className="absolute left-0 top-0 bottom-0 w-1 sm:w-1.5 rounded-l-2xl sm:rounded-l-3xl"                style={{ background: "linear-gradient(180deg, #2563eb 0%, #7c3aed 50%, #0891b2 100%)" }}              />              <div                className="absolute -top-16 -right-16 w-64 h-64 rounded-full pointer-events-none"                style={{ background: "radial-gradient(circle, rgba(37,99,235,0.12) 0%, transparent 70%)" }}              />              <div                className="absolute -bottom-12 left-1/3 w-48 h-48 rounded-full pointer-events-none"                style={{ background: "radial-gradient(circle, rgba(124,58,237,0.08) 0%, transparent 70%)" }}              />              <UIParticleCanvas />              <div className="relative z-10 px-6 sm:px-10 md:px-14 lg:px-16 py-8 sm:py-10 md:py-11">                <div className="inline-flex items-center gap-2 px-3 sm:px-4 py-1.5 rounded-full mb-6 sm:mb-8 bg-white border border-slate-200 shadow-inner">                  <ShieldCheck size={12} className="text-blue-500" />                  <span className="text-slate-500 text-[10px] font-black uppercase tracking-[0.3em]">                    User Interface & Experience Design                  </span>                </div>                <div className="grid grid-cols-1 md:grid-cols-2 gap-8 md:gap-12 lg:gap-16 items-center">                  <div>                    <h1                      className="font-black text-slate-950 leading-tight"                      style={{ fontSize: "clamp(1.75rem, 5vw, 3.5rem)", letterSpacing: "-0.03em" }}                    >                      System{" "}                      <span className="text-blue-600">Interfaces.</span>                    </h1>                  </div>                  <div className="flex flex-col gap-5">                    <p className="text-slate-600 text-sm sm:text-base md:text-lg leading-relaxed">                      The AquaSmart React dashboard serves as a centralized command center,                      providing instantaneous visualization of complex AIoT data through a                      mobile-first, responsive interface with real-time sub-150ms sync.                    </p>                    <div className="flex flex-wrap gap-2">                      {[                        { label: "Telemetry Dashboard",  bg: "#eff6ff", border: "#bfdbfe", color: "#2563eb" },                        { label: "LSTM Forecasting",     bg: "#f5f3ff", border: "#ddd6fe", color: "#7c3aed" },                        { label: "CNN Diagnostics",      bg: "#ecfeff", border: "#a5f3fc", color: "#0891b2" },                        { label: "Automation Control",   bg: "#ecfdf5", border: "#a7f3d0", color: "#059669" },                      ].map((chip, i) => (                        <span                          key={i}                          className="text-xs font-bold px-3 py-1.5 rounded-full"                          style={{ backgroundColor: chip.bg, border: `1px solid ${chip.border}`, color: chip.color }}                        >                          {chip.label}                        </span>                      ))}                    </div>                  </div>                </div>              </div>            </div>            {/* ── SCREENS DIVIDER ─────────────────────────── */}            <div className="flex items-center gap-3 mb-8 md:mb-10">              <div className="h-px flex-1 bg-slate-200" />              <span className="text-[10px] sm:text-xs font-semibold uppercase tracking-widest text-slate-400 px-2 text-center">                6 Interface Modules              </span>              <div className="h-px flex-1 bg-slate-200" />            </div>            {/* ── SCREEN CARDS GRID ───────────────────────── */}            <div className="grid grid-cols-1 sm:grid-cols-2 gap-5 sm:gap-6 lg:gap-8">              {screens.map((s, idx) => (                <ScreenCard                  key={idx}                  s={{ ...s, src: `${base}assets/${s.src}` }}                />              ))}            </div>          </div>        </div>      </PageTransition>    </Layout>  );}