import React from "react";import Layout from "../components/Layout.jsx";import PageTransition from "../components/PageTransition.jsx";import Section, { Card, Badge } from "../components/Section.jsx";import { Cpu, Wifi, Server, Database, Code2, BrainCircuit, ShieldCheck } from "lucide-react";export default function TechStack() {  const stack = [    { icon: <Cpu size={18} />, name: "ESP32-CAM", desc: "Edge gateway + camera + sensor polling." },    { icon: <Wifi size={18} />, name: "ngrok", desc: "Secure TCP/HTTP tunnel for remote access." },    { icon: <Server size={18} />, name: "Node.js", desc: "Traffic control, soft-sensors, automation logic, persistence." },    { icon: <BrainCircuit size={18} />, name: "Flask", desc: "AI engine hosting LSTM + MobileNetV2 inference APIs." },    { icon: <Database size={18} />, name: "Supabase", desc: "PostgreSQL + realtime subscriptions + storage buckets." },    { icon: <Code2 size={18} />, name: "React + Vite", desc: "Mobile-first dashboard, fast builds, GitHub Pages hosting." },    { icon: <ShieldCheck size={18} />, name: "Audit Logs", desc: "Automation_Logs for transparency & energy auditing." },  ];  return (    <Layout>      <PageTransition>        <Section          eyebrow="Implementation"          title="Technology Stack"          subtitle="Modern AIoT architecture optimized for realtime streaming, scalable persistence, and efficient deep learning inference."        >          <div className="grid gap-4 md:grid-cols-3">            {stack.map((s) => (              <Card key={s.name}>                <div className="flex items-center gap-2">                  <span className="text-blue-600">{s.icon}</span>                  <div className="text-sm font-extrabold">{s.name}</div>                </div>                <div className="mt-2 text-sm text-slate-600">{s.desc}</div>                <div className="mt-3"><Badge>Production-ready</Badge></div>              </Card>            ))}          </div>        </Section>      </PageTransition>    </Layout>  );}