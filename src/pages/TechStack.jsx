import React, { useState, useEffect, useRef } from "react";import Layout from "../components/Layout.jsx";import PageTransition from "../components/PageTransition.jsx";import { Card, Badge } from "../components/Section.jsx";import {  Cpu,  Wifi,  Server,  Database,  Code2,  BrainCircuit,  ShieldCheck,  Terminal,  Layers,  Globe,  Layers3,  Network,  FlaskConical,  LayoutDashboard,  HardDrive,  Zap,  GitBranch,  Activity,  Lock,  Radio,  ScanLine,  Microscope,  CloudCog,  Bolt,  Webhook,  PlugZap,  BarChart3,  FileStack,  CircuitBoard,} from "lucide-react";/* ── Particle Canvas ──────────────────────────────────────── */function TechParticleCanvas() {  const ref = useRef(null);  useEffect(() => {    const canvas = ref.current;    if (!canvas) return;    const ctx = canvas.getContext("2d");    let raf;    const resize = () => {      canvas.width = canvas.offsetWidth;      canvas.height = canvas.offsetHeight;    };    resize();    window.addEventListener("resize", resize);    const COUNT = 55;    const pts = Array.from({ length: COUNT }, () => ({      x: Math.random() * canvas.width,      y: Math.random() * canvas.height,      r: Math.random() * 2.8 + 0.8,      vx: (Math.random() - 0.5) * 0.4,      vy: (Math.random() - 0.5) * 0.4,      a: Math.random() * 0.6 + 0.2,    }));    const draw = () => {      ctx.clearRect(0, 0, canvas.width, canvas.height);      for (let i = 0; i < pts.length; i++) {        for (let j = i + 1; j < pts.length; j++) {          const dx = pts[i].x - pts[j].x;          const dy = pts[i].y - pts[j].y;          const dist = Math.sqrt(dx * dx + dy * dy);          if (dist < 160) {            ctx.beginPath();            ctx.strokeStyle = `rgba(37,99,235,${0.22 * (1 - dist / 160)})`;            ctx.lineWidth = 1.0;            ctx.moveTo(pts[i].x, pts[i].y);            ctx.lineTo(pts[j].x, pts[j].y);            ctx.stroke();          }        }      }      pts.forEach((p) => {        ctx.beginPath();        ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);        ctx.fillStyle = `rgba(59,130,246,${p.a * 0.7})`;        ctx.fill();        p.x += p.vx;        p.y += p.vy;        if (p.x < 0 || p.x > canvas.width) p.vx *= -1;        if (p.y < 0 || p.y > canvas.height) p.vy *= -1;      });      raf = requestAnimationFrame(draw);    };    draw();    return () => { cancelAnimationFrame(raf); window.removeEventListener("resize", resize); };  }, []);  return <canvas ref={ref} className="absolute inset-0 w-full h-full" />;}/* ── Data ─────────────────────────────────────────────────── */const techGroups = [  {    category: "Edge Perception Layer",    categoryIcon: CircuitBoard,    categoryColor: "#0891b2",    categoryBg: "#ecfeff",    categoryBorder: "#a5f3fc",    techs: [      {        name: "ESP32-CAM",        icon: Cpu,        subIcon: ScanLine,        desc: "High-performance MCU with integrated OV2640 camera module. Handles multi-parameter sensor polling and visual frame acquisition.",        tags: ["C++", "GPIO", "Hardware"],        color: "#0891b2",        lightBg: "#ecfeff",        borderColor: "#a5f3fc",        stat: "10s polling rate",      },      {        name: "Sensor Array",        icon: Activity,        subIcon: Radio,        desc: "Integration of pH, Temperature, Turbidity, and Salinity sensors for high-frequency environmental data fusion at 10-second intervals.",        tags: ["Analog", "I2C", "OneWire"],        color: "#0d9488",        lightBg: "#f0fdfa",        borderColor: "#99f6e4",        stat: "4 parameters",      },    ],  },  {    category: "Transmission & Connectivity",    categoryIcon: Network,    categoryColor: "#4f46e5",    categoryBg: "#eef2ff",    categoryBorder: "#c7d2fe",    techs: [      {        name: "ngrok Secure Tunnel",        icon: Lock,        subIcon: Globe,        desc: "Establishes a secure TCP/HTTP tunnel between local edge nodes and the public internet, eliminating the need for expensive static IPs.",        tags: ["Zero-Trust", "Tunneling"],        color: "#4f46e5",        lightBg: "#eef2ff",        borderColor: "#c7d2fe",        stat: "Zero-config setup",      },      {        name: "WebSockets",        icon: Wifi,        subIcon: PlugZap,        desc: "Full-duplex communication protocol for 150ms real-time data propagation and low-latency actuator command triggers.",        tags: ["Real-time", "Duplex"],        color: "#6366f1",        lightBg: "#f5f3ff",        borderColor: "#ddd6fe",        stat: "150ms latency",      },    ],  },  {    category: "Dual-Backend Intelligence",    categoryIcon: CloudCog,    categoryColor: "#059669",    categoryBg: "#ecfdf5",    categoryBorder: "#a7f3d0",    techs: [      {        name: "Node.js Orchestrator",        icon: Server,        subIcon: GitBranch,        desc: "Central traffic controller handling 10s data ingestion, Soft-Sensor mathematical derivation, and automation relay logic.",        tags: ["Express", "Scalable"],        color: "#059669",        lightBg: "#ecfdf5",        borderColor: "#a7f3d0",        stat: "750ms response",      },      {        name: "Flask AI Engine",        icon: FlaskConical,        subIcon: BrainCircuit,        desc: "Dedicated Python environment hosting LSTM and MobileNetV2 inference APIs, ensuring high-speed neural network execution.",        tags: ["TensorFlow", "Keras"],        color: "#7c3aed",        lightBg: "#f5f3ff",        borderColor: "#ddd6fe",        stat: "Dual AI models",      },    ],  },  {    category: "Data & UI Layer",    categoryIcon: Layers3,    categoryColor: "#2563eb",    categoryBg: "#eff6ff",    categoryBorder: "#bfdbfe",    techs: [      {        name: "Supabase (PostgreSQL)",        icon: Database,        subIcon: FileStack,        desc: "Cloud database with real-time CDC (Change Data Capture) and storage buckets for historical sensor logs and pathological imagery.",        tags: ["Persistence", "SQL"],        color: "#10b981",        lightBg: "#ecfdf5",        borderColor: "#a7f3d0",        stat: "17,000+ logs",      },      {        name: "React + Vite Dashboard",        icon: LayoutDashboard,        subIcon: BarChart3,        desc: "Mobile-first PWA dashboard using modern component architecture for real-time visualization and administrative override.",        tags: ["Tailwind", "Vite"],        color: "#2563eb",        lightBg: "#eff6ff",        borderColor: "#bfdbfe",        stat: "PWA ready",      },    ],  },];/* ── Tech Card ───────────────────────────────────────────── */function TechCard({ tech }) {  const [hovered, setHovered] = useState(false);  const Icon = tech.icon;  const SubIcon = tech.subIcon;  return (    <div      className="bg-white rounded-3xl border flex flex-col overflow-hidden transition-all duration-500 h-full"      style={{        borderColor: hovered ? tech.color : tech.borderColor,        boxShadow: hovered          ? `0 24px 48px rgba(0,0,0,0.08), 0 0 0 1px ${tech.color}22`          : "0 2px 12px rgba(0,0,0,0.04)",        transform: hovered ? "translateY(-6px)" : "translateY(0)",      }}      onMouseEnter={() => setHovered(true)}      onMouseLeave={() => setHovered(false)}    >      {/* Gradient top bar */}      <div        className="h-1.5 w-full"        style={{ background: `linear-gradient(90deg, ${tech.color}, ${tech.color}88)` }}      />      <div className="p-6 sm:p-7 flex flex-col flex-grow">        {/* ── ROW 1: Icon + Name + Live dot ── */}        <div className="flex items-center gap-4 mb-5">          {/* Primary icon */}          <div            className="w-12 h-12 rounded-2xl flex items-center justify-center border-2 flex-shrink-0 transition-all duration-300"            style={{              background: `linear-gradient(135deg, ${tech.lightBg}, white)`,              borderColor: hovered ? tech.color + "55" : tech.borderColor,              transform: hovered ? "scale(1.08)" : "scale(1)",              boxShadow: hovered ? `0 8px 20px ${tech.color}22` : "none",            }}          >            <Icon size={22} style={{ color: tech.color }} />          </div>          {/* Name + subtitle area */}          <div className="flex-1 min-w-0">            <h3              className="font-black text-slate-900 leading-tight"              style={{ fontSize: "clamp(1rem, 2vw, 1.15rem)", letterSpacing: "-0.01em" }}            >              {tech.name}            </h3>            {/* Tags inline below name */}            <div className="flex flex-wrap gap-1 mt-1.5">              {tech.tags.map((tag) => (                <span                  key={tag}                  className="text-[9px] font-black uppercase tracking-wider px-2 py-0.5 rounded-full"                  style={{                    background: `${tech.color}12`,                    color: tech.color,                  }}                >                  {tag}                </span>              ))}            </div>          </div>          {/* Live dot + stat — right aligned */}          <div className="flex flex-col items-end gap-1.5 flex-shrink-0">            <div className="flex items-center gap-1">              <div                className="h-1.5 w-1.5 rounded-full animate-pulse"                style={{ backgroundColor: tech.color }}              />              <span className="text-[8px] font-black uppercase tracking-widest text-slate-400">Live</span>            </div>            <span              className="text-[9px] font-bold px-2 py-0.5 rounded-full whitespace-nowrap"              style={{ background: `${tech.color}15`, color: tech.color }}            >              {tech.stat}            </span>          </div>        </div>        {/* ── Accent divider ── */}        <div          className="h-px mb-4 rounded-full transition-all duration-300"          style={{            backgroundColor: tech.color,            opacity: hovered ? 0.6 : 0.15,            width: hovered ? "44px" : "24px",          }}        />        {/* ── Description ── */}        <p className="text-sm sm:text-[15px] text-slate-800 leading-relaxed flex-grow">          {tech.desc}        </p>        {/* ── Footer ── */}        <div          className="mt-6 pt-4 border-t flex items-center justify-between"          style={{ borderColor: tech.borderColor }}        >          <div className="flex items-center gap-2">            {/* Secondary icon as a small badge */}            <div              className="w-6 h-6 rounded-lg flex items-center justify-center"              style={{ background: `${tech.color}15` }}            >              <SubIcon size={12} style={{ color: tech.color }} />            </div>            <span              className="text-[11px] font-semibold"              style={{ color: tech.color }}            >              Verified Module            </span>          </div>          <ShieldCheck size={14} style={{ color: tech.color, opacity: 0.5 }} />        </div>      </div>    </div>  );}/* ── Page ────────────────────────────────────────────────── */export default function TechStack() {  const footerTechs = [    { label: "ESP32-CAM", color: "#0891b2", bg: "#ecfeff", border: "#a5f3fc", icon: Cpu },    { label: "Node.js", color: "#059669", bg: "#ecfdf5", border: "#a7f3d0", icon: Server },    { label: "Flask + TensorFlow", color: "#7c3aed", bg: "#f5f3ff", border: "#ddd6fe", icon: FlaskConical },    { label: "Supabase", color: "#10b981", bg: "#ecfdf5", border: "#a7f3d0", icon: Database },    { label: "React + Vite", color: "#2563eb", bg: "#eff6ff", border: "#bfdbfe", icon: LayoutDashboard },    { label: "ngrok", color: "#4f46e5", bg: "#eef2ff", border: "#c7d2fe", icon: Lock },  ];  return (    <Layout>      <PageTransition>        <div className="bg-slate-50 min-h-screen">          <div className="mx-auto w-full max-w-[1400px] px-4 sm:px-6 lg:px-12 py-10 md:py-16 lg:py-20">            {/* ── HEADER ──────────────────────────────────── */}            <div              className="relative mb-12 md:mb-16 overflow-hidden rounded-2xl sm:rounded-3xl border border-slate-200 shadow-sm"              style={{ background: "linear-gradient(135deg, #ffffff 0%, #f8fafc 70%, #eff6ff 100%)" }}            >              <div                className="absolute left-0 top-0 bottom-0 w-1 sm:w-1.5 rounded-l-2xl sm:rounded-l-3xl"                style={{ background: "linear-gradient(180deg, #0891b2 0%, #4f46e5 50%, #2563eb 100%)" }}              />              <div className="absolute -top-16 -right-16 w-64 h-64 rounded-full pointer-events-none"                style={{ background: "radial-gradient(circle, rgba(59,130,246,0.10) 0%, transparent 70%)" }} />              <div className="absolute -bottom-12 left-1/3 w-48 h-48 rounded-full pointer-events-none"                style={{ background: "radial-gradient(circle, rgba(79,70,229,0.07) 0%, transparent 70%)" }} />              <TechParticleCanvas />              <div className="relative z-10 px-6 sm:px-10 md:px-14 lg:px-16 py-8 sm:py-10 md:py-11">                <div className="inline-flex items-center gap-2 px-3 sm:px-4 py-1.5 rounded-full mb-6 bg-white border border-slate-200 shadow-inner">                  <Layers size={12} className="text-blue-600" />                  <span className="text-slate-500 text-[10px] font-black uppercase tracking-[0.3em]">Technical Ecosystem</span>                </div>                <div className="grid grid-cols-1 md:grid-cols-2 gap-8 md:gap-12 lg:gap-16 items-center">                  <div>                    <h1 className="font-black text-slate-950 leading-tight"                      style={{ fontSize: "clamp(1.75rem, 5vw, 3.5rem)", letterSpacing: "-0.03em" }}>                      The <span className="text-blue-600">Technology</span> Stack.                    </h1>                  </div>                  <div className="flex flex-col gap-5">                    <p className="text-slate-600 text-sm sm:text-base md:text-lg leading-relaxed">                      AquaSmart leverages a heterogeneous stack optimized for high-velocity data ingestion, real-time cloud reasoning, and low-latency automated intervention.                    </p>                    <div className="flex flex-wrap gap-2">                      {[                        { label: "Edge Computing", bg: "#ecfeff", border: "#a5f3fc", color: "#0891b2" },                        { label: "Deep Learning", bg: "#f5f3ff", border: "#ddd6fe", color: "#7c3aed" },                        { label: "Real-time Stack", bg: "#eff6ff", border: "#bfdbfe", color: "#2563eb" },                      ].map((chip, i) => (                        <span key={i} className="text-xs font-bold px-3 py-1.5 rounded-full"                          style={{ backgroundColor: chip.bg, border: `1px solid ${chip.border}`, color: chip.color }}>                          {chip.label}                        </span>                      ))}                    </div>                  </div>                </div>              </div>            </div>            {/* ── STACK LAYERS ────────────────────────────── */}            <div className="space-y-12 md:space-y-14">              {techGroups.map((group, gIdx) => {                const CatIcon = group.categoryIcon;                return (                  <div key={gIdx}>                    <div className="flex items-center gap-3 mb-6 md:mb-8">                      <div                        className="w-9 h-9 rounded-xl flex items-center justify-center flex-shrink-0"                        style={{ backgroundColor: group.categoryBg, border: `1.5px solid ${group.categoryBorder}` }}                      >                        <CatIcon size={18} style={{ color: group.categoryColor }} />                      </div>                      <h2 className="text-xs sm:text-sm font-black uppercase tracking-[0.35em] whitespace-nowrap"                        style={{ color: group.categoryColor }}>                        {group.category}                      </h2>                      <div className="h-px w-full"                        style={{ background: `linear-gradient(90deg, ${group.categoryColor}50, transparent)` }} />                    </div>                    <div className="grid grid-cols-1 sm:grid-cols-2 gap-5 sm:gap-6 lg:gap-8">                      {group.techs.map((tech, tIdx) => (                        <TechCard key={tIdx} tech={tech} />                      ))}                    </div>                  </div>                );              })}            </div>            {/* ── FOOTER INTEGRITY CARD ───────────────────── */}            <div              className="mt-14 md:mt-20 rounded-2xl sm:rounded-3xl overflow-hidden shadow-xl relative"              style={{ background: "linear-gradient(135deg, #0f172a 0%, #1e1b4b 55%, #0f172a 100%)" }}            >              {/* Dot grid */}              <div className="absolute inset-0 pointer-events-none"                style={{                  backgroundImage: "radial-gradient(circle, rgba(255,255,255,0.05) 1px, transparent 1px)",                  backgroundSize: "24px 24px",                }} />              {/* Glow blobs */}              <div className="absolute -top-20 -right-20 w-72 h-72 rounded-full pointer-events-none"                style={{ background: "radial-gradient(circle, rgba(59,130,246,0.15) 0%, transparent 70%)" }} />              <div className="absolute -bottom-16 -left-16 w-56 h-56 rounded-full pointer-events-none"                style={{ background: "radial-gradient(circle, rgba(79,70,229,0.12) 0%, transparent 70%)" }} />              <div className="relative z-10 p-8 sm:p-10 md:p-12 lg:p-16">                <div className="grid grid-cols-1 md:grid-cols-2 gap-10 md:gap-16 items-center">                  {/* Left: text */}                  <div>                    {/* Icon */}                    <div                      className="w-14 h-14 rounded-2xl flex items-center justify-center mb-6 border border-white/10"                      style={{ backgroundColor: "rgba(255,255,255,0.08)" }}                    >                      <ShieldCheck size={26} className="text-blue-400" />                    </div>                    <p className="text-[10px] font-black uppercase tracking-[0.35em] text-blue-400 mb-2">                      Systems Integration                    </p>                    <h2 className="font-black text-white mb-5 leading-tight"                      style={{ fontSize: "clamp(1.4rem, 3vw, 2.2rem)", letterSpacing: "-0.02em" }}>                      Integration Integrity                    </h2>                    <div className="h-px w-12 mb-6 rounded-full"                      style={{ background: "linear-gradient(90deg, #3b82f6, #6366f1)" }} />                    <p className="text-slate-300 text-sm sm:text-base md:text-lg leading-relaxed italic">                      "Every technology in the AquaSmart stack was selected for its interoperability.                      From the ngrok TCP bridge to the Flask-AI API, the stack ensures 99.9% uptime                      and zero-loss persistence of 17,000+ environmental logs."                    </p>                  </div>                  {/* Right: tech cards grid */}                  <div className="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-2 lg:grid-cols-3 gap-3">                    {footerTechs.map((t, i) => {                      const TIcon = t.icon;                      return (                        <div                          key={i}                          className="flex flex-col items-center gap-2 p-4 rounded-2xl border text-center transition-all duration-200 hover:-translate-y-1"                          style={{                            backgroundColor: "rgba(255,255,255,0.06)",                            borderColor: "rgba(255,255,255,0.10)",                          }}                        >                          <div                            className="w-9 h-9 rounded-xl flex items-center justify-center"                            style={{ background: `linear-gradient(135deg, ${t.bg}, white)`, border: `1px solid ${t.border}` }}                          >                            <TIcon size={16} style={{ color: t.color }} />                          </div>                          <span className="text-[11px] font-bold text-white/80 leading-tight text-center">                            {t.label}                          </span>                        </div>                      );                    })}                  </div>                </div>              </div>            </div>          </div>        </div>      </PageTransition>    </Layout>  );}